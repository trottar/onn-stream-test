# 2026-09-16 — D4 Linux runtime reconciliation

<!-- PRIVYHUB_D4_RUNTIME_RECONCILIATION_2026_09_16:DAILY:BEGIN -->
This record reconciles the newer D4 Linux debug session and the temporary
Windows/ExpressVPN routing follow-up against the 2026-09-15 durable memory.
Where it conflicts with older summary bullets, this newer runtime evidence is
authoritative.

## Android signing migration

A Linux-built APK install returned:

`INSTALL_FAILED_UPDATE_INCOMPATIBLE`

The package already installed on the onn and the Linux-built APK were signed by
different certificates.

Observed SHA-256 certificate digests:

- installed package:
  `8412dfec1a9ed77ade7a0f20070f1347716eaf175686319b9d841669364a257a`
- Linux build:
  `455bde4effefd7421a0733884189fc8e2e75b388252853022df5a83fec1c2f4c`

Classification: packaging/signing migration issue, separate from runtime
streaming behavior.

## Linux audio characterization attempt

The fixed 5500-kbps characterization probe returned:

`C3_FIXED_5500_START_BLOCKED`

Immediate reason:

`process audio is not active`

The managed PulseAudio monitor configuration existed, but active capture and
packet emission were absent in that probe. Audio characterization was not
continued because video/network/controller isolation became higher priority.
This does not invalidate the previously validated Linux PulseAudio architecture.

## Linux hard-freeze observation

The Linux PC hard-froze while RetroArch was running and the onn game stream had
been paused/stale for roughly fifteen minutes.

Possible memory/GPU/driver causes were considered, but available kernel evidence
did not establish a root cause. No memory leak was confirmed. Preserve this as
an observation only.

## Persistent uinput and realtime-priority fix

The udev ownership rule already existed, but `/dev/uinput` was not reliably
registered after reboot because the module was not guaranteed to load.

Persistent module loading was added:

`/etc/modules-load.d/99-privyhub-uinput.conf`

containing:

`uinput`

After reboot, `/dev/uinput` existed with PrivyHub ownership and the required
realtime-priority allowance remained available (`ulimit -r` reported `1`).

Classification: persistent controller prerequisites resolved. Older memory
listing these as open production-permission gaps is superseded for the current
development host.

## Temporary routed topology and ExpressVPN packet filter

Tested path:

`GL-iNet -> Wi-Fi -> Windows PC -> Ethernet -> Linux PC`

Windows could directly reach the Linux companion listener, but onn/GL-iNet
traffic did not consistently reach Linux.

ICS rebinding produced unstable alternating behavior and was rejected as the
solution.

Packet-level Windows tracing showed GL-iNet packets arriving on Wi-Fi and
reaching the Windows filtering/TCP-IP layers without corresponding Ethernet
egress. Disabling Windows Firewall did not repair the path.

Adapter inspection then found the ExpressVPN NDIS packet filter
`expressvpn-pkf` attached to both physical adapters. Disabling that binding on
the Wi-Fi and Ethernet adapters immediately restored the routed path.

After that correction:

- companion connected;
- Crash Bandicoot launched;
- native game streaming worked.

Classification: ExpressVPN physical-adapter filtering was a real routed-path
blocker in this temporary topology.

## Controller isolation

Initial symptom: controller buttons worked, movement did not.

Transport diagnostics showed thousands of controller packets with zero send
errors and nonzero motion events.

Linux created:

`PrivyHub Virtual Gamepad P1`

RetroArch detected it in port 1.

The generated autoconfig mapped the left stick to axis 0/1, and the virtual
device exposed `ABS_X`, `ABS_Y`, `ABS_RX`, `ABS_RY`, `ABS_Z`, `ABS_RZ`,
`ABS_HAT0X`, and `ABS_HAT0Y`.

Live event monitoring showed changing `EV_ABS` values on `ABS_X` and `ABS_Y`,
including near-full-scale positive and negative motion.

Therefore the remaining movement problem is not Android transport, Linux
permissions, uinput creation, or analog generation. The next investigation is
RetroArch PS1 controller mode/core/session behavior: digital PlayStation pad
versus DualShock/analog mode.

## rtw88/LPS split retained from the audit note

A power-save test successfully disabled ordinary LPS with no LPS/register
failure during the run, but streaming still failed severely:

- `SndbufErrors +3026`
- `RcvbufErrors +20726`
- audio `send_errors=3026`

Conclusion: the rtw88 LPS fault is real, but it is not the primary explanation
for the failed stream run. Do not elevate LPS-off into the product fix.

## ExpressVPN follow-up: VPN tunnel versus downstream Linux Internet

The physical `expressvpn-pkf` bindings were confirmed disabled on both Wi-Fi and
Ethernet.

With ExpressVPN disconnected:

- Windows Internet worked;
- Linux Internet worked;
- PrivyHub local routing worked.

With ExpressVPN connected:

- Windows Internet worked;
- Linux could still reach its Windows gateway;
- Linux external Internet routing failed.

Windows IP forwarding remained enabled in both states. No active `NetNat`
object or ICS sharing configuration was present.

Route inspection showed the Windows Internet route using the physical Wi-Fi
path with the VPN disconnected and the ExpressVPN interface with the VPN
connected.

Classification: forwarded Linux traffic does not successfully traverse the
ExpressVPN tunnel in this temporary Windows-as-router topology. This is deferred
by design. When Linux requires upstream Internet during development, disconnect
ExpressVPN.

No literal network addresses are retained in durable memory.

## Current D4 position

Working:

- Linux companion service;
- routed local path with physical ExpressVPN filter bindings disabled;
- game launch and native streaming;
- persistent uinput prerequisite;
- persistent realtime-priority prerequisite;
- controller button transport;
- analog event generation.

Active next issue:

- RetroArch PS1 analog controller mode/session configuration.

Separate/deferred:

- saved Opal UDP burst/gap pathology;
- Windows/ExpressVPN downstream Internet forwarding;
- Linux hard-freeze root cause unless it recurs;
- rtw88 LPS as a primary root-cause theory.

Do not reopen ICS, uinput permissions/module loading, realtime-priority
persistence, Windows Firewall, or the temporary ExpressVPN bridge without new
evidence.
<!-- PRIVYHUB_D4_RUNTIME_RECONCILIATION_2026_09_16:DAILY:END -->

<!-- PRIVYHUB_D084_LINUX_A8_PLATFORM_ADAPTER:DAILY:BEGIN -->
## D-084 — Linux A8 named-profile portability bug

A8.2 was originally designed and runtime-validated on:

`Android canonical XUSB/PHI1 -> Windows ViGEm X360 -> RetroArch XInput`.

D-076 later migrated Linux controller output to PrivyHub uinput devices and
project-owned udev autoconfig while intentionally preserving canonical PHI1/XUSB
semantics.

The A8 schema/editor survived that migration correctly because it stores
semantic control names. The final runtime adapter did not: it still converted
semantic names through `A8_XINPUT_*` numeric tables on Linux.

Repository evidence:

- Linux uinput maps physical A/B/X/Y to
  BTN_SOUTH/BTN_EAST/BTN_WEST/BTN_NORTH.
- The validated Linux RetroArch udev profile resolves canonical physical
  A/B/X/Y to joystick indices 0/1/3/2.
- Linux LT/RT are axes +2/+5.
- Linux right stick X/Y are axes 3/4.
- Linux D-pad is axes 6/7.
- Linux Y axes use the opposite raw sign from the old XInput adapter because
  D-076 inverts canonical XInput Y when emitting Linux ABS_Y/ABS_RY.

The existing A8 deterministic probe also retained Windows expectations, so it
could not detect this Linux portability regression.

Decision: preserve A8 storage/editor semantics and add a host-specific
translation table at the final RetroArch adapter boundary. Update the existing
probe rather than creating a parallel profile system or diagnostic path.

Status after patch packaging: deterministic/source validation complete;
real onn gameplay validation pending.
<!-- PRIVYHUB_D084_LINUX_A8_PLATFORM_ADAPTER:DAILY:END -->

<!-- PRIVYHUB_D085_LINUX_UDEV_HAT_MAPPING:DAILY:BEGIN -->
## D-085 — older-memory controller reconciliation and udev D-pad fix

The controller regression was re-audited against the older Phase-A/D-076 record.

Key correction to prior reasoning:
- Windows controller behavior had real gameplay acceptance.
- Linux D-076 host validation was narrower. P1-P4 were created/configured,
  PHI1 updates arrived, meta controls worked, Save/Load worked, and teardown was
  graceful, but D-076 still required real gameplay controls for full acceptance.

Raw Baseline 42 remains useful only at its measured layer. It observed:
- ABS_X/Y -> joystick axes 0/1
- ABS_Z -> 2
- ABS_RX/RY -> 3/4
- ABS_RZ -> 5
- ABS_HAT0X/Y -> 6/7

The project incorrectly copied the hat indexes into RetroArch udev autoconfig as
ordinary directional axes. RetroArch udev uses `h0up/down/left/right` for those
hat directions.

D-085 corrects the four project udev profiles and the D-084 Linux A8 translator.
No transport or uinput event-generation change is made.

This explains the observed Crash split without requiring a transport theory:
face buttons can work while D-pad movement is absent. Analog movement in a PS1
title remains a separate Digital/DualShock controller-mode question.
<!-- PRIVYHUB_D085_LINUX_UDEV_HAT_MAPPING:DAILY:END -->

<!-- PRIVYHUB_D086_CONTROLLER_PARITY_CHECKPOINT:DAILY:BEGIN -->
## D-086 — controller parity runtime acceptance and multiplayer handoff

After installing D-085 and restarting the companion, integrated gameplay was
tested with three different games and three different input profiles. The user
reported all three ran correctly.

Classification:

`LINUX_GAMEPLAY_CONTROLLER_PARITY_CONFIRMED_FOR_TESTED_PROFILES`

This closes the Linux gameplay-input regression that had been misclassified
during D-076 as fully validated from host-side configuration alone. D-076 remains
valid for host integration; D-085 supplies the missing gameplay proof.

The effective fix chain is:

- D-076: Linux uinput backend and project-owned udev autoconfig;
- D-076R1/R2: managed autoconfig path/runtime corrections;
- D-084R1: platform-aware A8 source-token translation;
- D-085: correct RetroArch udev hat representation for D-pad in both default
  autoconfig and Linux A8 translation;
- D-086: runtime-evidence/memory checkpoint only.

A separate Linux regression remains: PS1 multitap/multiplayer does not work.
Historical Windows evidence proved the intended Port-1-only manual multitap path
and four-player CTR behavior. The next diagnostic should reuse that evidence
contract while replacing Windows XInput slot inspection with Linux uinput /
RetroArch port evidence.

No production behavior is changed by D-086.
<!-- PRIVYHUB_D086_CONTROLLER_PARITY_CHECKPOINT:DAILY:END -->

<!-- PRIVYHUB_D087_LINUX_PS1_MULTITAP_CONFIG_PATH:DAILY:BEGIN -->
## D-087 — Linux PS1 multitap Config-path portability

With Multitap enabled, launch failed before RetroArch with:
`Beetle PSX HW core-options file is unavailable for multitap launch`.

Audit evidence found no managed `rgui_config_directory` and found the active
Linux Beetle seed at `~/.config/retroarch/config/Beetle PSX HW/Beetle PSX HW.opt`,
with Port 1 and Port 2 multitap keys present and disabled.

Current source still used `runtime executable parent / config / Beetle PSX HW`
and required that path under the project root. Decision: preserve Windows
behavior and add Linux XDG/default-user Config-root selection. Existing `.opt`
transformation remains unchanged.

Runtime validation pending.
<!-- PRIVYHUB_D087_LINUX_PS1_MULTITAP_CONFIG_PATH:DAILY:END -->

<!-- PRIVYHUB_D087R1_LINUX_PS1_MULTITAP_METADATA_PATH:DAILY:BEGIN -->
## D-087R1 — Linux multitap metadata-path follow-up

After D-087 selected the correct external Linux RetroArch Config tree, runtime
failed later in `_prepare_ps1_multitap_options()` because `options_file`
metadata still used `target_options.relative_to(self.project_root)`.

The `.opt` had already been materialized and byte-verified. R1 changes only
metadata serialization: Windows stays project-relative; Linux is represented as
`retroarch-config/...` relative to its validated Config root.

Runtime validation pending.
<!-- PRIVYHUB_D087R1_LINUX_PS1_MULTITAP_METADATA_PATH:DAILY:END -->

<!-- PRIVYHUB_D088_MULTITAP_RUNTIME_VALIDATION:DAILY:BEGIN -->
## D-088 — Linux PS1 multitap runtime acceptance

After D-087 and D-087R1, Crash Bash was launched on the Linux runtime with
PrivyHub Multitap enabled.

User runtime result:
- game launch succeeded;
- four players were available;
- each remote/controller independently controlled a player.

Classification:
`LINUX_PS1_MULTITAP_FOUR_PLAYER_PARITY_CONFIRMED`

This reproduces the historical Windows Phase-A Port-1 multitap behavior on the
Linux migration target and closes the active multiplayer regression.

No production behavior is changed by D-088; it is a durable-evidence/status
checkpoint only.
<!-- PRIVYHUB_D088_MULTITAP_RUNTIME_VALIDATION:DAILY:END -->

<!-- PRIVYHUB_D089_D5_EXTERNAL_VOD_STORAGE_CONSTRAINT:DAILY:BEGIN -->
## D-089 — D5 external VOD storage constraint

Before starting the final D4 Games regression, the Phase-D roadmap was reviewed.

The current Linux Prototype 1's internal disk is small relative to the VOD/movie
library. During D5, bulk VOD content will therefore be hosted temporarily on a
simple external hard drive until later server-like storage infrastructure is
built.

This is recorded as an architecture/deployment constraint, not a production
implementation change.

D5 requirements added:
- configurable bulk-media root;
- no requirement to duplicate large media onto the internal/project disk;
- lightweight metadata/catalog/cache may remain internal;
- external-root absence must be distinguishable from actual library deletion;
- avoid hard-coding the temporary external-drive arrangement into the long-term
  storage architecture.

Immediate execution order remains:
`D4 final Games regression -> D5 media/server restoration`.
<!-- PRIVYHUB_D089_D5_EXTERNAL_VOD_STORAGE_CONSTRAINT:DAILY:END -->

<!-- PRIVYHUB_D090_D4_LINUX_GAMES_ACCEPTANCE:DAILY:BEGIN -->
## D-090 — D4 final Linux Games regression accepted

The first guided D4 regression probe successfully completed library metadata,
SNES, and full PS1 lifecycle coverage, then aborted at the cheat stage because a
single read-only loopback status request failed. This was classified as a probe
robustness defect rather than a Games regression.

R1 preserved the earlier passing evidence, added bounded retries only for
idempotent status GETs, and resumed at the unfinished stages.

Final R1 results:
- prior library/SNES/PS1 evidence accepted with zero required-line gaps;
- cheat stage passed;
- IPS mod stage passed;
- named input-profile stage passed;
- restart/recovery stage passed;
- `git diff --check` clean;
- no transient API retries were needed in R1.

Final classification:
`D4_FINAL_GAMES_REGRESSION_CONFIRMED_WITH_NO_FIXTURE_SKIPS`

Coverage:
- NES: zero library fixtures, skipped;
- Genesis: zero library fixtures, skipped;
- SNES: runtime validated by D4 run;
- PS1: runtime validated across normal lifecycle plus previously accepted
  controller/multitap paths.

D4 is complete. D5 media/server restoration is next.
<!-- PRIVYHUB_D090_D4_LINUX_GAMES_ACCEPTANCE:DAILY:END -->

<!-- PRIVYHUB_D091_D5_MEDIA_BASELINE:DAILY:BEGIN -->
## D-091 — D5 Linux media baseline and symlink boundary

D5 baseline audit against checkpoint `c7fe53a613d55f9d38b37b16609c590118465efd`
found:
- companion status/catalog reachable;
- 91 dynamic VOD sources;
- VOD Range fixture returned HTTP 206;
- health, telemetry, Self-Test and IPTV category endpoints reachable;
- project media root remains on the internal filesystem;
- browser/camera source runners remain PowerShell.

The user clarified that the external movie library is currently linked beneath
`media/vod/movies`.

A targeted symlink probe selected a representative movie and measured:
- symlink target available;
- direct read through symlink true;
- direct read through resolved target true;
- effective read access true;
- exact catalog path present;
- one-byte port-8000 HTTP Range true with 206 and Content-Range.

Classification:
`D5_VOD_SYMLINK_HOST_RANGE_PATH_HEALTHY`.

The current onn playback failure is therefore downstream of host storage,
catalog, and HTTP Range serving. Next diagnostic is the onn request boundary.
<!-- PRIVYHUB_D091_D5_MEDIA_BASELINE:DAILY:END -->

<!-- PRIVYHUB_D092_DYNAMIC_VOD_SYMLINK_HEALTH:DAILY:BEGIN -->
## D-092 — dynamic external-VOD source-start health

D-091 request-boundary probe observed zero port-8000 requests during Aviator
failure.

Android logcat then exposed the preceding boundary:
`POST /sources/<dynamic-vod-id>/start` returned HTTP 503 with
`VOD source is unavailable`.

Source inspection found the mismatch in `_vod_is_healthy()`:
dynamic scanner `_filesystem_path` is resolved, so a movie beneath a VOD
directory symlink resolves to external storage and fails the blanket
`relative_to(MEDIA_ROOT.resolve())` check.

D-092 narrows the fix to scanner-generated dynamic VOD and preserves the static
source containment rule.
<!-- PRIVYHUB_D092_DYNAMIC_VOD_SYMLINK_HEALTH:DAILY:END -->

<!-- PRIVYHUB_D093R2_D5_EXTERNAL_VOD_RUNTIME_VALIDATION:DAILY:BEGIN -->
## D-093 / D-093R1 / D-093R2 — external-VOD runtime validation checkpoint

D-092 installed successfully.

Post-restart probe:
`D092_DYNAMIC_VOD_SOURCE_START_CONFIRMED`

Measured source-start:
- representative catalog source found;
- POST source start HTTP 200;
- `ready: true`;
- no source-start error.

The representative external movie was then tested through the normal onn UI and
played successfully. Continue Watching was explicitly tested and works.

Checkpoint attempt history:
1. D-093 rolled back because its roadmap transformer expected `### D5` while
   the authoritative roadmap uses `## D5`.
2. D-093R1 corrected the heading but rolled back because its post-write check
   expected a marker that the inserted roadmap block did not contain.
3. D-093R2 embeds and validates the same exact roadmap marker.

Both earlier attempts rolled back cleanly and retained no project modifications.

Conclusion:
D-092 is runtime validated and the old HTTP 503 dynamic-VOD health failure is
closed.

Next:
implement the proper configurable bulk-media-root boundary rather than promoting
the temporary VOD symlink into product architecture.
<!-- PRIVYHUB_D093R2_D5_EXTERNAL_VOD_RUNTIME_VALIDATION:DAILY:END -->

<!-- PRIVYHUB_D094_EXTERNAL_STORAGE_REMOUNT:DAILY:BEGIN -->
## D-094 external-storage remount behavior

Fresh D5 runtime evidence clarified the remaining external-VOD storage boundary.

Observed sequence:
1. external drive ejected;
2. VOD symlink target became unavailable;
3. `/sources` correctly returned zero VOD sources;
4. drive was physically reconnected but remained unmounted;
5. opening the drive in the Linux file manager caused the OS desktop storage
   layer to mount it;
6. the existing VOD symlink immediately became valid again;
7. PrivyHub dynamic rescanning repopulated the movie library without a companion
   restart;
8. onn movie playback worked normally again.

Interpretation:
- PrivyHub dynamic catalog recovery is already working when the configured
  filesystem path becomes available again;
- the missing behavior is deterministic OS-level storage mounting, not catalog
  rescanning;
- the appliance must not depend on a desktop file-manager click to activate
  removable storage.

Next D5 storage implementation should separate responsibilities:
- OS/service layer owns deterministic mounting at a stable path;
- PrivyHub owns explicit bulk-media-root configuration and availability state;
- unavailable configured storage must be distinguishable from an empty library;
- when the root returns, normal rescanning should repopulate automatically.
<!-- PRIVYHUB_D094_EXTERNAL_STORAGE_REMOUNT:DAILY:END -->

<!-- PRIVYHUB_D096_CONFIGURABLE_VOD_STORAGE:DAILY:BEGIN -->
## D-095 / D-096 storage boundary

D-095 classified the current external VOD filesystem as:
`D5_STORAGE_STABLE_ID_READY_DESKTOP_AUTOMOUNT_CONFIRMED`.

The filesystem has stable identity and hotplug support, but was mounted through
the desktop/user automount layer and had no existing `/etc/fstab` ownership.

D-096 implements the first-class storage boundary:
- local configurable VOD root;
- stable logical `/vod` namespace and IDs;
- separate internal live root;
- multi-root Linux byte-range serving;
- API availability state;
- deterministic UUID-based system automount helper.

Runtime/E2E validation remains required before D-096 is accepted.
<!-- PRIVYHUB_D096_CONFIGURABLE_VOD_STORAGE:DAILY:END -->

<!-- PRIVYHUB_D096R1_STORAGE_DEVICE_DISCOVERY:DAILY:BEGIN -->
## D-096R1 — storage configurator discovery fix

D-096 production patch installed successfully.

The root storage migration attempt then rolled back with:
`blkid -s UUID -o value systemd-1`.

This identified an installer/helper defect:
findmnt returned the synthetic systemd automount source, not the physical block
partition.

D-096R1 changes the helper to identify the real block partition by filesystem
major:minor from `stat()` and `lsblk`.

No production media behavior is changed by R1.
<!-- PRIVYHUB_D096R1_STORAGE_DEVICE_DISCOVERY:DAILY:END -->

<!-- PRIVYHUB_D096R2R1_PROBE_INSTALLER_PRESTATE:DAILY:BEGIN -->
## D-096R2R1 — layered pre-state fix

D-096R2 failed before modification because its pre-state checker forgot the
still-uncommitted D-096 production stack and considered those expected changes
unexpected.

Rejected legitimate files included the D-096 companion/range-server changes and
three D-096 memory files.

D-096R2R1 merges D-096 and D-096R1 receipts when validating expected local
tracked state. Newer R1 hashes supersede D-096 hashes on overlapping files.
<!-- PRIVYHUB_D096R2R1_PROBE_INSTALLER_PRESTATE:DAILY:END -->

<!-- PRIVYHUB_D096R3_PROBE_LISTENER_LIFECYCLE:DAILY:BEGIN -->
## D-096R3 — listener/process lifecycle classifier

The post-probe diagnostic showed no remaining PrivyHub processes, listeners, or
port owners on 8765/8000.

The R2R1 probe was therefore wrong to use raw bindability as lifecycle success.

A second probe bug was also confirmed:
`D096_STORAGE_BOUNDARY_NOT_CONFIRMED` returned code 0 because the code used
`endswith("_CONFIRMED")`.

D-096R3 uses process/listener ownership and exact success-token equality.
<!-- PRIVYHUB_D096R3_PROBE_LISTENER_LIFECYCLE:DAILY:END -->

<!-- PRIVYHUB_D097_VOD_APPLIANCE_MODE:DAILY:BEGIN -->
## D-097 — zero-touch removable VOD appliance mode

The first hotplug test manually stopped both mount and automount units. That was
safe for removal but disabled automatic recovery. After reinsertion, restarting
the automount and accessing the stable mount immediately restored the movie
library and playback in the onn GUI.

D-097 removes that operational mistake from the product model:
- automount stays active permanently;
- normal media serving is read-only;
- no Linux command is required to unplug/reinsert in normal media mode.

A separate writable maintenance mode remains future work for direct ingest/rip.
<!-- PRIVYHUB_D097_VOD_APPLIANCE_MODE:DAILY:END -->

<!-- PRIVYHUB_D098_ABSENT_VOD_CATALOG_RESILIENCE_V1:DAILY:BEGIN -->
## D-098 — absent external-VOD scanner resilience

Physical external-drive removal caused repeated request-thread tracebacks:

`OSError: [Errno 19] No such device:
'/mnt/privyhub-media/onn-stream-test/media/vod'`

The exception occurred at `disk_root.exists()` in `_scan_media_directory()`.

A saved Continue Watching action also attempted the absent source and Companion
became unavailable through the same scanner failure.

Reinserting the drive restored Companion, confirming mount identity and logical
VOD mapping are healthy.

D-098 makes dynamic filesystem OSError a clean unavailable-library state and
protects both catalog refresh and stale source resolution.
<!-- PRIVYHUB_D098_ABSENT_VOD_CATALOG_RESILIENCE_V1:DAILY:END -->

<!-- PRIVYHUB_D099_NONBLOCKING_VOD_PRESENCE_V1:DAILY:BEGIN -->
## D-099 — nonblocking VOD presence

Measured with disk absent: companion process alive; 8765 listening; `/status` 3.043s; `/sources` >12s; stale Aviator start >12s; `/status` after 1.967s; `/sources` after >12s; port 8000 not listening. The remaining failure was automount activation latency, not a companion crash. D-099 checks backing-device presence without touching the automount and keeps Range server startup independent of removable VOD.
<!-- PRIVYHUB_D099_NONBLOCKING_VOD_PRESENCE_V1:DAILY:END -->

### D-099R1 — predecessor receipt ordering

D-099 failed before modification because its pre-state merge handled all
required patches first and optional D-097 last. That let an older D-097 memory
hash override newer D-098 state. D-099R1 merges successful receipts
chronologically so the newest installed state wins.

<!-- PRIVYHUB_D100_D5_EXTERNAL_VOD_HOTPLUG_RUNTIME_ACCEPTANCE:DAILY:BEGIN -->
## D-100 — external VOD hotplug runtime acceptance

Final onn test passed:

1. external disk unplugged;
2. Companion remained usable;
3. VOD showed no movies;
4. client refresh succeeded;
5. disk reinserted without Linux/file-manager interaction;
6. movies returned automatically;
7. Continue Watching appeared and resumed successfully;
8. disk unplugged a second time;
9. VOD again showed no movies;
10. client refresh again succeeded.

This closes the D5 removable/external VOD storage seam at runtime.

Next D5 work: Linux browser/camera runners.
<!-- PRIVYHUB_D100_D5_EXTERNAL_VOD_HOTPLUG_RUNTIME_ACCEPTANCE:DAILY:END -->

<!-- PRIVYHUB_D101R1_BROWSER_CAMERA_C6_RECLASSIFICATION:DAILY:BEGIN -->
## D-101 — browser/camera reclassified to C6

Roadmap review confirmed that C6 already defines the generalized native source
abstraction for Games, browser/app, cameras/live, and later sources.

Decision:
do not port the old Windows browser/camera runners as D5 Linux parity work.

Instead:
- finish D5 with VOD + Live TV/EPG + diagnostics;
- finish D7/D8;
- return to remaining Phase C on Linux;
- implement browser/app and camera/live sources through C6.

Potential browser control target:
Bluetooth keyboard/mouse paired to the onn/client and forwarded through the
PrivyHub session/input boundary.

Broader smart-home camera integration remains Phase I.
<!-- PRIVYHUB_D101R1_BROWSER_CAMERA_C6_RECLASSIFICATION:DAILY:END -->

### D-101R1 local-roadmap correction

The earlier D-101 attempt failed before modification because its
installer incorrectly required `ROADMAP.md` at the Linux repository
root. The roadmap file reviewed in chat was uploaded reference material;
the repo's durable roadmap state is maintained in
`docs/memory/roadmap/STATUS.md`.

D-101R1 records the same accepted reclassification entirely through the
actual durable-memory/decision files and does not create or require a
new root-level roadmap file.

<!-- PRIVYHUB_D102_D5_TV_SYNC_CHECKPOINT:DAILY:BEGIN -->
## D5 Live TV/EPG runtime acceptance and TV-state ownership — 2026-09-16

The onn Live TV subsystem was inspected against the current repository rather
than the older companion-only IPTV plugin path.

Observed current TV status:
- English / All Countries;
- 3356 streams;
- 22 favorites;
- 49 reliable;
- 4 hidden;
- 3 enabled providers;
- catalog refreshed 2026-09-16 15:55:38 local;
- 2 EPG mappings;
- 0 cached programmes.

Normal Live TV categories/navigation and channel playback work on the onn. The
Program Guide control also exists and executes. However, no tested channel has
actual guide listings.

Result:
- D5.1 Live TV catalog/categories/playback accepted.
- EPG data not accepted.

Repository inspection confirmed the mature TV state is primarily onn-local:
`privyhub_tv.db` contains provider/channel/user-state data and
`TvRepository.exportSettingsJson()` already serializes the important portable
user state. Linux still has an older lightweight IPTV-org plugin and is not the
durable authority for the mature TV state.

Architecture decision D-102:
- Linux becomes durable authority for TV user intent;
- onn keeps a local cache;
- use a versioned serialized sync contract rather than copying SQLite DB files;
- catalogs/EPG are derived cache;
- runtime health/recent-viewing observations need explicit merge semantics.

D5 was subdivided D5.1 through D5.9. Immediate next work is D5.2: one
diagnostic-only EPG ingestion probe that identifies the first failing stage
before any production EPG change.

### D-102R1 installer delivery correction

The first D-102 delivery failed before modification. The pasted shell wrapper
used interactive-shell-fatal error handling, so the installer never ran and the
worktree remained at `53fd9b1`. The worktree also already contained unrelated
untracked `_patches/` and `_probes/` directories. D-102R1 preserves the D-102
documentation payload, uses a shell-safe invocation, and allows only those known
untracked directory prefixes while continuing to reject tracked/unexpected
worktree changes.
<!-- PRIVYHUB_D102_D5_TV_SYNC_CHECKPOINT:DAILY:END -->

<!-- PRIVYHUB_D103_D5_EPG_INGESTION_PROBE:DAILY:BEGIN -->
## D-103 D5.2 EPG diagnostic prepared

After D-102R1 was pushed, source inspection confirmed two important current
behaviors in `TvEpgRepository`:
- any nonzero mapping cache can suppress mapping refresh for 24 hours;
- `getGuide()` catches mapping/XML exceptions and falls back to the cached guide.

D-103 therefore adds a diagnostic-only Linux probe rather than changing
production behavior. It reproduces Android mapping acceptance, compares catalog
identity, optionally snapshots onn TV/EPG SQLite state read-only through ADB
`run-as`, and samples bounded XMLTV programme matching.

The first D-103 installer delivery correctly rolled back before commit because
`git diff --check` detected an extra blank line at EOF in this daily-memory
append path. D-103R1 corrects only that installer append behavior and adds an
exact regression test; the diagnostic probe and production scope are unchanged.

Runtime result is pending.
<!-- PRIVYHUB_D103R1_STAGED_WHITESPACE_REPAIR:BEGIN -->
### D-103R1 staged-validation follow-up

D-103R1 installed successfully into the worktree, but the shell wrapper stopped
before commit because `git diff --cached --check` found trailing Markdown
whitespace in the two newly added D-103 memory records.

The whitespace was repaired in place before commit. The diagnostic probe,
production scope, and D5 technical state were unchanged.
<!-- PRIVYHUB_D103R1_STAGED_WHITESPACE_REPAIR:END -->

<!-- PRIVYHUB_D103_D5_EPG_INGESTION_PROBE:DAILY:END -->
