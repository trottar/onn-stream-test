The authoritative handoff note is `handoffs/CURRENT_HANDOFF.md`.
The authoritative resumable state is `CURRENT.md`.
The compact Phase C brief is `PHASE_C_CONTEXT.md`.

Phase C Linux continuation active.

Validated: C1 profile/backend, C2 telemetry, and the Linux encoder-only actuator.
Classified: Linux is `video_only_restart`, not authorized for automatic in-game
adaptation (`C3.L2`).
Open: `C3.L2a` — the 287-318 ms figure is not established as actuator cost, and
the decoder report evicted the evidence.
Next: `C3.L2b` decoder-report cycle retention.
Preserve patch workflow.
