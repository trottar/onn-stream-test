# 2026-09-15 — Phase D Linux baseline

Phase D Linux baseline work began from synchronized checkpoint
`894136397d9e1a8a3e8c8c22ba7c193b6dd17b9c`.

Validated host/runtime foundation:

- Debian 13.7, kernel 6.12.107+deb13-amd64.
- Ryzen 5 PRO 4650G, Renoir `amdgpu`.
- VAAPI H.264 encode verified by real 720p60 hardware encode.
- Stock FFmpeg exposes x11grab, kmsgrab, VAAPI and required encoding support.
- X11 is the validated desktop session.
- Exact-window x11grab -> VAAPI works for terminal and real RetroArch gameplay.
- Minimized/unmapped exact-window capture fails; mapped occlusion remains live.
- PulseAudio application-isolation architecture validated.
- uinput kernel support validated, including four simultaneous gamepads.
- RetroArch sees four pre-created PrivyHub pads distinctly.
- udev autoconfig discovery/matching validated.
- Project-owned RetroArch 1.22.2 Linux AppImage staged.
- FCEUmm, bsnes, BlastEm and Beetle/Mednafen PSX HW Linux cores staged and loaded.
- Real SNES content/video/audio runtime validated.
- Real RetroArch window -> x11grab -> 1280x720 VAAPI H.264 validated.
- Current native/emulator Python modules all import on Linux.
- NativeStreamManager constructs and reports status safely on Linux, but native
  backend fields remain Windows-specific.

Baseline 26 was intentionally not run. Discovery of the external-drive
authoritative Windows project data made creation of a synthetic fresh RetroArch
data tree the wrong next step.

The external backup supplied authoritative persistent RetroArch state.
Windows-specific generated config was excluded. Portable saves, states,
cheat profiles, mod profiles, controller overrides and state-slot metadata were
migrated byte-for-byte.

Migration:
- 129 files;
- source/target manifests identical;
- manifest SHA-256:
  `6de3fa8b67d5a0ee21ace20af9347b5cf0f5997f3a9d410fcd23d6410b18280c`.

A Linux project-owned RetroArch config was then established using Linux paths,
PulseAudio and project save/state/system directories.

Baseline 27 validated:
- ready=True;
- RetroArch installed=True;
- RetroArch configured=True;
- no missing cores;
- no runtime error.

Baseline 28 launched real SNES content through the existing EmulatorManager.

Observed:
- network command state PLAYING;
- game remained active;
- SAVE_FILES confirmed;
- POSIX SIGTERM shutdown was graceful;
- network QUIT fallback not needed;
- manager became inactive after stop.

Architectural disposition:
the emulator lifecycle/control layer is portable enough to preserve.
The remaining Linux work is backend substitution around the existing control
surface rather than a separate Linux emulator stack.

Next:
establish the smallest production native-video backend seam, then Linux audio,
controller output, telemetry, permissions and migrated-data regressions.
