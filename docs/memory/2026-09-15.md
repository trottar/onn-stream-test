---
memory_schema: 1
as_of: 2026-09-15
---

# 2026-09-15

Consolidated 2026-09-18. Two dated files existed for this date — one at the
memory root and one under `docs/memory/memory/` — recording different work.
Both bodies are preserved verbatim below under their original titles; nothing
was summarized, reordered or dropped. Dated history now lives in one place,
`docs/memory/YYYY-MM-DD.md`.

## Phase D Linux baseline

Phase D Linux baseline work began from synchronized checkpoint
`894136397d9e1a8a3e8c8c22ba7c193b6dd17b9c`.

Validated host/runtime foundation:

- Debian 13.7, kernel 6.12.107+deb13-amd64.
- Ryzen 5 PRO 4650G, Renoir `amdgpu`.
- VAAPI H.264 encode verified by real 720p60 hardware encode.
- Stock FFmpeg exposes x11grab, kmsgrab, VAAPI and required encoding support.
- X11 is the validated desktop session.
- Exact-window x11grab -> VAAPI works for terminal and real RetroArch gameplay.
- Minimized/unmapped exact-window capture fails; mapped occlusion remains live.
- PulseAudio application-isolation architecture validated.
- uinput kernel support validated, including four simultaneous gamepads.
- RetroArch sees four pre-created PrivyHub pads distinctly.
- udev autoconfig discovery/matching validated.
- Project-owned RetroArch 1.22.2 Linux AppImage staged.
- FCEUmm, bsnes, BlastEm and Beetle/Mednafen PSX HW Linux cores staged and loaded.
- Real SNES content/video/audio runtime validated.
- Real RetroArch window -> x11grab -> 1280x720 VAAPI H.264 validated.
- Current native/emulator Python modules all import on Linux.
- NativeStreamManager constructs and reports status safely on Linux, but native
  backend fields remain Windows-specific.

Baseline 26 was intentionally not run. Discovery of the external-drive
authoritative Windows project data made creation of a synthetic fresh RetroArch
data tree the wrong next step.

The external backup supplied authoritative persistent RetroArch state.
Windows-specific generated config was excluded. Portable saves, states,
cheat profiles, mod profiles, controller overrides and state-slot metadata were
migrated byte-for-byte.

Migration:
- 129 files;
- source/target manifests identical;
- manifest SHA-256:
  `6de3fa8b67d5a0ee21ace20af9347b5cf0f5997f3a9d410fcd23d6410b18280c`.

A Linux project-owned RetroArch config was then established using Linux paths,
PulseAudio and project save/state/system directories.

Baseline 27 validated:
- ready=True;
- RetroArch installed=True;
- RetroArch configured=True;
- no missing cores;
- no runtime error.

Baseline 28 launched real SNES content through the existing EmulatorManager.

Observed:
- network command state PLAYING;
- game remained active;
- SAVE_FILES confirmed;
- POSIX SIGTERM shutdown was graceful;
- network QUIT fallback not needed;
- manager became inactive after stop.

Architectural disposition:
the emulator lifecycle/control layer is portable enough to preserve.
The remaining Linux work is backend substitution around the existing control
surface rather than a separate Linux emulator stack.

Next:
establish the smallest production native-video backend seam, then Linux audio,
controller output, telemetry, permissions and migrated-data regressions.

## D-076 Linux controller preparation and development patch

Baselines 42-45 validated the Linux controller architecture before production
change: exact uinput map, PHI1 translation, four-pad RetroArch autoconfig, and
project-relative autoconfig resolution. D-076 then adds the Linux uinput backend
inside `NativeControllerBridge`, preserving Windows ViGEm and PHI1 v1.

Status after installation: development patch; real gameplay/meta-hotkey runtime
validation pending.

## D-076R1 managed RetroArch autoconfig-path correction

D-076 isolated runtime validation passed, but the first real managed RetroArch
probe exposed a session-path issue rather than a controller-mapping failure.
All four Linux uinput pads existed before launch and RetroArch selected the udev
joypad driver, but P1-P4 were reported `not configured`.

Fresh evidence established the cause: the persistent config kept the portable
relative setting `joypad_autoconfig_dir = "data/games/retroarch/autoconfig"`,
while EmulatorManager launches RetroArch with `cwd=executable.parent`. RetroArch
therefore resolved that relative path below the AppImage directory, where no
autoconfig profiles exist. The project-owned autoconfig directory itself was
present and contained all four D-076 profiles.

D-076R1 keeps the persistent config portable. On Linux, only the generated
per-launch session config rewrites the single validated project-relative
`joypad_autoconfig_dir` to its absolute project-owned path. Windows behavior,
PHI1, uinput mapping, Android, video/FEC, audio, and emulator process cwd remain
unchanged.

Status: **DEVELOPMENT PATCH / MANAGED RETROARCH RUNTIME REVALIDATION PENDING**

## D-076R2 missing `sys` import correction

The first D-076R1 managed RetroArch revalidation failed before RetroArch launch.
Linux controller preflight succeeded (`linux_uinput`, four players), but the new
D-076R1 session-config branch referenced `sys.platform` without importing the
standard-library `sys` module. Cleanup removed all virtual pads correctly.

D-076R2 adds only the missing top-level `import sys` to
`companion/games/emulator_manager.py`. The D-076R1 autoconfig path-resolution
logic, D-076 uinput backend/mapping, PHI1, Android, video/FEC, D-075R1 audio,
RetroArch persistent config/profiles, process cwd, and runtime descriptor are
unchanged.

Status: **DEVELOPMENT PATCH / MANAGED RETROARCH RUNTIME REVALIDATION PENDING**

## D-076/R1/R2 host-side managed runtime reconciliation

Fresh managed RetroArch evidence closes the host-side controller runtime gate.
P1-P4 autoconfigured in ports 1-4, live PHI1 updates were clean, Pause/Resume
worked, RetroArch logged a real 284304-byte `.state` save and load, production
`EmulatorManager.stop()` completed `SAVE_FILES` -> `OK` and graceful SIGTERM
return code 0, and cleanup removed all virtual pads. The probe final `False` was
a classifier defect (`.state.png` match plus non-production `quit` requirement).
Full Android/onn Linux E2E remains pending.

## D-077 Linux platform-aware RetroArch runtime selection

A source-level audit before integrated onn E2E identified one remaining product
path gap: `GamesPlugin` uses the default `EmulatorManager`, but the tracked base
emulator descriptor still contains Windows executable/core names. The prior
Linux host probes used temporary Linux descriptors and therefore did not prove
normal product runtime selection.

D-077 adds a Linux override inside the existing trusted descriptor and merges
that override into EmulatorManager's effective config. The effective systems
mapping is essential because `_resolve_game()` reads core names from
`runtime["config"]["systems"]`.

A successful D-077 installation requires compile/JSON checks, an isolated
Linux-vs-Windows selection fixture, a live read-only normal default-manager
Linux readiness check, and `git diff --check`. Full onn E2E remains next.

<!-- PRIVYHUB_D078_DAILY -->
## D-078 — Linux companion media-server startup

The first normal companion launch after D-077 exposed an unconditional `powershell.exe` dependency in `PrivyHubController.start_server()`. Source inspection showed that `scripts/start_server.ps1` only wraps the portable Python `companion/range_server.py`. D-078 made this launcher platform-aware while preserving Windows behavior. Installer validation exercised the actual Linux `start_server()` branch on a loopback-only ephemeral port, confirmed HTTP 200 readiness, and cleanly stopped the managed process. Status: host startup seam validated; onn E2E still pending.

## Development history — E2E diagnosis and transport branch

<!-- PRIVYHUB_D079_MEMORY_CHECKPOINT_LINUX_ONN_STREAM_DIAGNOSIS_2026_09_15:DAILY:BEGIN -->
## Linux/onn integrated E2E diagnosis

The first real integrated Linux/onn game launch crossed the emulator/runtime
boundary successfully: PS1 launched, an existing save loaded, Linux controller
preflight worked under the temporary uinput ACL, the exact managed X11 window
was found, and VAAPI encoded the native stream near the intended 60 fps.

Android did not automatically enter the native stream because MainActivity
still requires the Windows-only `host_window_policy.window_found` preflight.
Linux's native backend later found the correct X11 window, proving this is a
separate handoff compatibility bug.

After manual entry, NativeStreamActivity failed stabilization with severe
transport symptoms. Fresh decoder evidence showed substantial video loss,
audio loss/starvation, stale-output drops, low delivered/rendered FPS and long
output/decode gaps. Host telemetry then showed a video send-call maximum near
785 ms. The independent Linux audio sender, already nonblocking and SCHED_RR
priority 1, simultaneously accumulated large send-error counts and long cadence
gaps.

Repeated `/proc/net/snmp` before/after probes showed thousands of UDP
`SndbufErrors` and `RcvbufErrors` per short run while interface
`tx_errors`/`tx_dropped` remained unchanged. Increasing `rmem_max`/`wmem_max`
to 4 MiB so 2 MiB application socket requests were honored did not improve the
stream and worsened sender behavior, falsifying small socket ceilings as the
root cause.

The host Wi-Fi adapter was identified as USB Realtek RTL8822BU (`0bda:b812`),
driver `rtw_8822bu`, at 5000 Mb/s USB speed. USB runtime autosuspend is disabled
and has never suspended. Disabling rtw88 deep-LPS did not materially change the
failure. Kernel logs showed repeated `firmware failed to leave lps state` plus a
register read timeout. A controlled NetworkManager profile test successfully
disabled ordinary Wi-Fi powersave; LPS/register warnings disappeared during
that run, but UDP send/receive-buffer errors and audio send failures remained
severe. Ordinary LPS is therefore a real secondary driver issue but not the
primary cause of stream collapse.

The next diagnostic is the preserved standalone Linux -> onn idle UDP acceptance
probe, outside RetroArch/MediaCodec/production streaming. The first Linux shell
wrapper failed before sending with `ONN_ADDRESS_DISCOVERY_FAILED`; correct only
the local address-discovery seam, without asking for or exposing an IP.

Separate cleanup: PS1 runtime reported missing `scph5501.bin`; restore it before
final PS1 acceptance, but it is not responsible for current stream transport
failure.

<!-- PRIVYHUB_D079_MEMORY_CHECKPOINT_LINUX_ONN_STREAM_DIAGNOSIS_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D079R1_MEMORY_CHECKPOINT_LINUX_IDLE_UDP_TEST_A_2026_09_15:DAILY:BEGIN -->
### Standalone Linux -> onn idle UDP Test A

The preserved transport laboratory was successfully run on the representative
Linux + home Opal + onn path after correcting local onn-address discovery.

Linux sender:

- 4000 planned attempts over 20 s at 5 ms;
- 3993 successful sends;
- 7 would-block;
- p95 send interval ~5.015 ms;
- max send call ~128.8 us.

Android:

- 3993 unique arrivals;
- no successfully sent unique packet was missing;
- 2626 duplicate arrivals, all carrying the same host packet stamp as their
  original;
- kernel arrival p95 ~18.36 ms;
- kernel max gap ~792.30 ms;
- 2116 clean host 4-6 ms intervals transformed to kernel <2 ms;
- 190 clean host 4-6 ms intervals transformed to kernel >=20 ms.

Linux kernel counters over the probe:

- `SndbufErrors +7`;
- `RcvbufErrors +0`.

This is decisive because the severe burst/gap and duplicate behavior exists
without RetroArch, video encode, production audio, controller traffic, or
native-stream load. It satisfies the deferred transport investigation's Linux
resume criterion. The prior Windows-era low-latency Wi-Fi-lock experiment
already failed to improve the same forward-path class, so the next accepted
probe is reverse idle traffic rather than retesting that mitigation.

<!-- PRIVYHUB_D079R1_MEMORY_CHECKPOINT_LINUX_IDLE_UDP_TEST_A_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D079R2_MEMORY_CHECKPOINT_LINUX_REVERSE_IDLE_UDP_TEST_B_2026_09_15:DAILY:BEGIN -->
### Standalone onn -> Linux idle UDP Test B

The preserved reverse UDP diagnostic was run with Android as a nonblocking
DatagramChannel sender and Linux as the receiver.

Measurements:

- Android attempts: 4000;
- Android successful sends: 4000;
- Linux unique arrivals: 3894;
- missing on Linux: 106;
- Linux duplicate arrivals: 476;
- same-stamp duplicates: 476;
- Android send p95 ~6.275 ms;
- Android send max ~29.424 ms;
- Linux receive p95 ~18.143 ms;
- Linux receive max ~173.722 ms;
- clean sender 4-6 ms -> Linux <2 ms: 1312;
- clean sender 4-6 ms -> Linux >=20 ms: 126;
- Linux UDP `SndbufErrors +0`;
- Linux UDP `RcvbufErrors +120`.

This confirms the representative environment reproduces the deferred transport
pathology bidirectionally without game/native-stream load. Because the reverse
sender is Android and Linux records no send-buffer errors in this direction, a
Linux-only sender fault cannot explain the shared behavior.

The next localization boundary is the home Opal/router path if a router-side
capture mechanism is available.

<!-- PRIVYHUB_D079R2_MEMORY_CHECKPOINT_LINUX_REVERSE_IDLE_UDP_TEST_B_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D079R3_MEMORY_CHECKPOINT_OPAL_SSH_CAPABILITY_2026_09_15:DAILY:BEGIN -->
### Opal SSH capability probe

A read-only capability check against the locally discovered default gateway
found:

- gateway discovery OK;
- SSH client available on Linux;
- gateway TCP/22 open;
- batch authentication unavailable / requires authentication.

No address was printed or persisted. No router setting was modified.

The next step is authenticated interactive shell capability inspection for
`tcpdump` and sanitized interface names.

<!-- PRIVYHUB_D079R3_MEMORY_CHECKPOINT_OPAL_SSH_CAPABILITY_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D079R4_MEMORY_CHECKPOINT_OPAL_SSH_RSA_NEGOTIATION_2026_09_15:DAILY:BEGIN -->
### Opal SSH host-key negotiation

The authenticated capability probe exited 255 before authentication with:

`no matching host key type found. Their offer: ssh-rsa`

This establishes an SSH host-key algorithm compatibility issue between the
modern Linux client and the router. No credentials were tested and no router
setting was changed.

The next attempt will opt into `ssh-rsa` for that invocation only.

<!-- PRIVYHUB_D079R4_MEMORY_CHECKPOINT_OPAL_SSH_RSA_NEGOTIATION_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D079R5_MEMORY_CHECKPOINT_OPAL_AUTH_CAPABILITIES_2026_09_15:DAILY:BEGIN -->
### Authenticated Opal capability inspection

Per-command `ssh-rsa` host-key compatibility allowed successful authenticated
root shell access to the Opal.

The sanitized result confirmed:

- router shell works;
- `tcpdump` is not installed;
- `iw` is installed;
- `br-lan` is the bridge;
- `wlan0` and `wlan1` are wireless interfaces.

No router setting or package was changed. Before installing a capture utility,
the next probe will verify the real firmware/package-manager environment and
free writable storage.

<!-- PRIVYHUB_D079R5_MEMORY_CHECKPOINT_OPAL_AUTH_CAPABILITIES_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D079R6_MEMORY_CHECKPOINT_OPAL_CAPTURE_READINESS_2026_09_15:DAILY:BEGIN -->
### Opal capture-readiness inspection

Authenticated router inspection found an OpenWrt/LEDE system with `opkg`,
`libpcap`, ~82,976 KiB free overlay, and zero cached package lists. `tcpdump`
is not currently installed.

`br-lan` bridges `eth0.1`, `wlan0`, and `wlan1`. `wlan0` is an AP on channel 1
and `wlan1` is an AP on channel 40.

No router package/configuration change was made.

The next diagnostic will use temporary RAM-only package indexes and restore the
empty cache, while also identifying which AP radio serves each PrivyHub endpoint
without printing network identifiers.

<!-- PRIVYHUB_D079R6_MEMORY_CHECKPOINT_OPAL_CAPTURE_READINESS_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D079R7_MEMORY_CHECKPOINT_OPAL_SSH_COMMAND_TRANSPORT_2026_09_15:DAILY:BEGIN -->
### Opal SSH command transport

A narrow SSH control probe established that quoted remote command execution
works with the existing per-invocation `ssh-rsa` host-key compatibility and
interactive password authentication.

The earlier exit-255 package/radio probe was therefore caused by its heredoc/stdin
remote-script transport form. No router package/configuration change occurred in
that failed attempt.

<!-- PRIVYHUB_D079R7_MEMORY_CHECKPOINT_OPAL_SSH_COMMAND_TRANSPORT_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D080_OPAL_DUAL_BOUNDARY_PROBE_01_2026_09_15:DAILY:BEGIN -->
### Opal package/radio mapping

Temporary package-index refresh confirmed both `tcpdump-mini` and `tcpdump`
candidates, and the package-list cache returned to zero files.

Endpoint mapping:

- Linux: `wlan0`;
- onn: `wlan1`.

D080 installs a diagnostic-only dual-radio capture/analyzer; production
streaming code remains unchanged.

<!-- PRIVYHUB_D080_OPAL_DUAL_BOUNDARY_PROBE_01_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D080R1_OPAL_DUAL_BOUNDARY_ANALYZER_FAIL_CLOSED_2026_09_15:DAILY:BEGIN -->
### D080 first runtime capture — invalid classifier result

The first Opal dual-radio capture restored router state and completed, but the
router evidence was not valid for localization.

The endpoint probe produced 2971 successful host sends, 2971 unique Android
arrivals, zero missing unique sends, and 5291 Android duplicates. Both router
PCAP analyses nevertheless found zero UTP1 sequences.

The analyzer incorrectly interpreted zero router observations as clean router
boundaries. That conclusion is withdrawn.

D080R1 makes the analyzer fail closed and reuses the existing PCAP files to
determine whether they contain zero records or unrecognized link-layer records.

<!-- PRIVYHUB_D080R1_OPAL_DUAL_BOUNDARY_ANALYZER_FAIL_CLOSED_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D080R2_MEMORY_CHECKPOINT_EMPTY_OPAL_PCAPS_2026_09_15:DAILY:BEGIN -->
### D080R1 existing-capture reanalysis

The corrected analyzer was run against the original D080 session without new
network traffic.

Both Opal PCAPs were exactly 24 bytes and contained zero records. This confirms
that the radio capture boundary itself saw no frames; the problem was not the
UTP1 parser.

The endpoint result from the same run remains valid: 2971 successful host sends,
2971 unique Android arrivals, 5291 Android duplicates, zero unique loss.

The unsupported downstream-of-router classification is withdrawn. Next work
moves to read-only inspection of Opal acceleration/fast-path state.

<!-- PRIVYHUB_D080R2_MEMORY_CHECKPOINT_EMPTY_OPAL_PCAPS_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D081_OPAL_ACCELERATION_OFF_DUAL_BOUNDARY_PROBE_2026_09_15:DAILY:BEGIN -->
### Opal acceleration state

Read-only inspection found both UCI flow-offload flags enabled and `sfhnat` /
`cls_flow` loaded. D081 adds a reversible acceleration-off repeat of D080 with a
router-side restore watchdog.

<!-- PRIVYHUB_D081_OPAL_ACCELERATION_OFF_DUAL_BOUNDARY_PROBE_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D081R1_MEMORY_CHECKPOINT_ACCELERATION_OFF_FALSIFIED_2026_09_15:DAILY:BEGIN -->
### D081 acceleration-off experiment

The controlled Opal acceleration-off test completed with explicit restoration.

With both OpenWrt UCI flow-offload flags disabled, the synthetic forward UDP
test still showed severe pathology: 3131 successful host sends, 2944 unique
Android arrivals, 5224 duplicates, and 187 missing successful unique packets.

Both per-radio tcpdump captures again contained zero packet records. `sfhnat`
remained loaded.

The original 1/1 offload configuration was restored and verified.

This falsifies the UCI flow-offload switches as the primary root cause or useful
workaround. Investigation moves to the Siflower vendor fast path.

<!-- PRIVYHUB_D081R1_MEMORY_CHECKPOINT_ACCELERATION_OFF_FALSIFIED_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D081R2_MEMORY_CHECKPOINT_SIFLOWER_INVENTORY_2026_09_15:DAILY:BEGIN -->
### Siflower fast-path inventory

Read-only router inventory confirmed the proprietary Siflower FMAC, RF,
Ethernet-switch, netlink, factory-read, and HNAT modules are loaded.

`sfhnat` exposes no module parameters. The standard `bridge` utility is absent.

The attempted installed-package subsection failed with an awk syntax error and
is explicitly excluded from evidence.

Next work remains read-only: map loaded module files to package ownership and
metadata.

<!-- PRIVYHUB_D081R2_MEMORY_CHECKPOINT_SIFLOWER_INVENTORY_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D082_OPAL_BRIDGE_BOUNDARY_PROBE_2026_09_15:DAILY:BEGIN -->
### Siflower ownership and D082

Read-only ownership mapping confirmed package ownership for HNAT, Ethernet
switch, netlink, RF, and factory-read components. The high/low-band FMAC
modules are loaded but were not found as standalone module files through normal
lookup.

The Wi-Fi side is sufficiently coupled that module unloading is deferred.

D082 adds a `br-lan`-only capture probe to determine whether the Linux bridge
master can observe the same synthetic traffic that per-radio tcpdump could not.

<!-- PRIVYHUB_D082_OPAL_BRIDGE_BOUNDARY_PROBE_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D083_OPAL_NETDEV_COUNTER_PROBE_2026_09_15:DAILY:BEGIN -->
### D082 bridge capture result

`br-lan` was completely invisible to tcpdump during active synthetic traffic:
its PCAP was only the 24-byte global header with zero packet records.

Endpoint traffic remained active:
3661 successful host sends, 3661 unique Android arrivals, 2371 Android
duplicates, zero unique loss.

D083 moves to read-only netdev counter accounting.

<!-- PRIVYHUB_D083_OPAL_NETDEV_COUNTER_PROBE_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D083R1_NETDEV_SAMPLER_LIFETIME_FIX_2026_09_15:DAILY:BEGIN -->
### D083 initial runtime failure

The first netdev-counter run did not yield enough router samples for analysis.
The router sampler setup reported OK, but the retrieved CSV had fewer than two
rows. Source inspection showed the background sampler was not detached from the
SSH setup session.

A separate runner bug masked the analyzer exception and returned exit 0.

D083R1 fixes sampler lifetime and fail-closed exit handling; no network or
production behavior is changed.

<!-- PRIVYHUB_D083R1_NETDEV_SAMPLER_LIFETIME_FIX_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_D083_CLOSEOUT_INVALID_DIAGNOSTICS_2026_09_15:DAILY:BEGIN -->
### D083/D083R1 diagnostic invalidation and closeout

D083 produced insufficient router counter samples and no valid networking
evidence. Its wrapper also masked analyzer failure with exit 0.

D083R1 attempted to detach the sampler with `nohup`, but the representative
Opal has neither a `nohup` command nor a BusyBox `nohup` applet. It failed at
setup with `sampler_not_running` and produced no measurement.

A read-only smoke control wrote the expected four CSV lines when the tiny
sampler was invoked through `sh`.

The smoke control also exposed a classifier defect: it printed
`tmp_noexec=YES`, while the raw mount line showed `/tmp` as
`rw,nosuid,nodev,noatime` with no `noexec`. Raw evidence wins; the classifier
is invalid.

D082 remains the last valid transport result. The router-specific investigation
is paused rather than extended.

<!-- PRIVYHUB_D083_CLOSEOUT_INVALID_DIAGNOSTICS_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_MEMORY_NORMALIZATION_D083_2026_09_15:DAILY -->
## Durable-memory normalization after D083 closeout

Core continuation files were normalized so current state no longer competes with
stale embedded "next" statements. Phase D Linux migration remains active;
D073-D078 host/runtime seams are retained as validated at their documented
scope; the representative UDP pathology has been reproduced bidirectionally;
D082 is the last valid router-boundary result; D083/D083R1 are invalid; and the
Opal/Siflower reverse-engineering branch is paused.

The next narrow production patch is the confirmed Android/Linux automatic
stream-handoff compatibility bug. No runtime, router, stream, emulator, or
network behavior is changed by this memory cleanup.

<!-- PRIVYHUB_ADB_RECOVERY_PROBE_LINUX_PARITY_2026_09_15:DAILY:BEGIN -->
### Linux ADB recovery-probe parity patch prepared

D4 setup verified the Linux companion itself healthy (process, TCP 8765, TCP
8000, localhost status, no active host firewall service). The privacy-safe ADB
v2 audit independently measured zero online/offline/unauthorized transports and
zero TLS-connect services. Review of D-053 and `build_install_onn.ps1` showed the
probe was incomplete: it audited discovery but did not execute the installer's
private cached-target/mDNS/reconnect/server-restart recovery sequence.

`privyhub_adb_recovery_probe_linux_parity_01_2026-09-15` updates only the ADB
diagnostic, adds a Linux-private XDG/user-state target cache, preserves endpoint
redaction, and updates stale literal installer checks. Off-device compile,
self-test and installer validation are performed before delivery. Runtime ADB
recovery remains pending on the authoritative Linux host.

<!-- PRIVYHUB_ADB_RECOVERY_PROBE_LINUX_PARITY_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_ADB_EPHEMERAL_PORT_RECOVERY_2026_09_15:DAILY:BEGIN -->
### Wireless-ADB ephemeral-port hypothesis and v4 probe

The Linux wireless-ADB branch was narrowed further. Debian Platform Tools
34.0.5 observed no TLS-connect service; forcing libadbmdns still returned zero.
A temporary Google Platform Tools 37.0.1 run reported `mdns_enabled: true` and
`mdns_backend: LIBADBMDNS` but also zero TLS-connect services. ADB version/backend
replacement is therefore not the current fix.

Windows history provides counter-evidence against blaming the permanent 2.4/5
GHz radio split: D-053 previously recovered the paired onn, including the
`cached-after-server-restart` branch. The stronger hypothesis is endpoint
lifetime: pairing remains durable while Android's secure wireless-ADB TLS server
can restart on a new random port.

`privyhub_adb_ephemeral_port_recovery_probe_01_2026-09-15` extends only the
existing diagnostic. From a stale/private host it scans ports on that single
host, attempts ADB only on listening candidates, validates through the existing
pairing, refreshes private caches, and keeps all network-bearing values out of
shareable evidence. Runtime validation is pending.

<!-- PRIVYHUB_ADB_EPHEMERAL_PORT_RECOVERY_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_ADB_EPHEMERAL_PORT_RECOVERY_V5_2026_09_15:DAILY:BEGIN -->
### Wireless-ADB v4 runtime failure and v5 correction

The v4 ephemeral-port recovery probe did not recover the paired onn. The user
then connected locally with `adb connect` using the current private endpoint;
connection succeeded immediately. This is authoritative evidence that pairing
and network reachability were intact and that v4's bootstrap/discovery logic was
the failure.

While connected, `service.adb.tls.port` returned no value. The onn kernel reported
an ephemeral range of 32768-60999. V5 now caches host/range state from a valid
online session, restricts future stale-endpoint scanning to that one host and
range, and prompts for repair/re-pair followed by one retry only after automatic
recovery is exhausted. Production installer parity remains deferred pending
runtime validation.

<!-- PRIVYHUB_ADB_EPHEMERAL_PORT_RECOVERY_V5_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_ADB_ENDPOINT_DEBUG_V6_2026_09_15:DAILY:BEGIN -->
### ADB endpoint-debug escalation

V5 automatic wireless-ADB recovery did not work on the representative Linux/onn
setup. Manual `adb connect` to the current endpoint still succeeds. A diagnostic
patch adds `--debug-endpoints` so the exact cache/host/port selection and ADB
connect/get-state result can be observed locally. No production installer or
application behavior is changed. Do not persist literal endpoint values in
memory/evidence.

<!-- PRIVYHUB_ADB_ENDPOINT_DEBUG_V6_2026_09_15:DAILY:END -->

<!-- PRIVYHUB_ADB_ENDPOINT_WATCH_V7_2026_09_15:DAILY:BEGIN -->
### ADB endpoint scanner watch-port diagnostic

The endpoint-debug run showed only cached/open-candidate paths, not every TCP
port traversed. A targeted watch-port option was added so a locally known current
ADB port can prove whether the scanner actually schedules and tests that port.
No production recovery behavior is changed.

<!-- PRIVYHUB_ADB_ENDPOINT_WATCH_V7_2026_09_15:DAILY:END -->

<!-- D4_LINUX_HANDOFF_FIX_01_DAILY -->
## D4 Linux handoff realignment

- Two installer attempts rolled back safely: first because `gradlew` lacked executable permission, second because the Linux Android toolchain was not installed.
- Linux build environment was then established and the unchanged app produced `BUILD SUCCESSFUL`.
- v3 reached Kotlin compilation but rolled back on an invalid regex escape in the redaction helper; v4 removes the regex and uses exact configured-host replacement. v4 still gates modification on a successful pre-patch baseline build.
