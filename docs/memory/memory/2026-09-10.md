---
memory_schema: 1
as_of: 2026-09-10
baseline_commit: 25e9a1492a684dbaeebede90ea7ca4abd3eab1fb
---

# Session Memory — 2026-09-10

## Context

A8 was checkpointed and pushed as `25e9a14`. The user requested a detailed project/chat/repository audit and an OpenClaw-inspired durable memory tree under `docs/memory`, with future ZIPs carrying memory updates.

## Audit inputs

- Current project handoff and recent project-chat summaries.
- Pushed GitHub checkpoint `25e9a14` and recent commit history.
- Root/docs/companion/scripts/tools repository structure.
- Current controller sender/bridge source.
- Existing `DEBUGGING_MEMORY.md`, `DEBUGGING_HARNESS.md`, roadmap/status/known-issues/A8 docs, and repository audit.
- Existing runtime-validation conclusions from Phase A.

## Main findings

1. The technical architecture is increasingly coherent; the largest continuity risk is documentation freshness and append-only historical state.
2. Older roadmap/status files lag the current A8-complete checkpoint.
3. `docs/DEBUGGING_MEMORY.md` is valuable but mixes intermediate and final states; retain it as historical evidence and use curated/current/daily separation going forward.
4. Source and generated user/runtime data are well separated by repository hygiene.
5. Sunshine/Moonlight, clean-machine runtime bootstrap, security hardening, and conventional CI remain explicit future debt—not four-player scope.
6. Controller source audit shows PHI1 is 36-byte one-player-per-packet with a player index; current two-player limits are counts/state arrays, making PHI1-preserving four-player extension the narrowest current hypothesis.
7. The A8 controller-preflight ordering fix is a non-negotiable preservation boundary.

## Memory-system decision

`docs/memory` becomes the repository-backed durable learning/logging/chat communication layer. Core files stay curated; dated session logs keep detail; decisions/evidence retain provenance; the current handoff is rewritten as state changes.

Actual ChatGPT account Memory is unavailable in this project, so no claim is made that this information was written outside the repository.

## Next

Install/commit this documentation-only memory bootstrap without touching production behavior. Then resume the four-player source/probe workflow from the exact local `25e9a14` tree.

## Four-player exact-local audit and base patch

The diagnostic-only source audit ran against local HEAD `25e9a1492a684dbaeebede90ea7ca4abd3eab1fb` and modified no production files. It reported `EXPECTED_TWO_PLAYER_BASELINE_PROTOCOL_REUSABLE`: Android `PLAYER_COUNT=2`, Windows `MAX_PLAYERS=2`, PHI1 allocation/struct size 36, literal two-element bridge bookkeeping, and no P3/P4 profile/editor references. Exact audited production hashes are recorded in `evidence/FOUR_PLAYER_SOURCE_AUDIT_2026-09-10.md`.

Decision D-007 is now active for development: preserve PHI1 v1 and generalize the existing canonical sender/ViGEm layer to four. The base patch intentionally leaves RetroArch/A8 P3/P4 profile/editor behavior unchanged until four slots are proven. A durable host probe was added for four XInput slots and synthetic P1-P4 routing. Runtime validation remains pending.

The memory-bootstrap install also exposed a PowerShell packaging lesson: a standalone `finally` entered after a completed `try/catch` is invalid. Future complete command blocks must keep cleanup syntactically attached or explicit.

## Four-player base-slot runtime result

The isolated base-slot probe returned `FOUR_PLAYER_BASE_SLOTS_CONFIRMED`. With no baseline host XInput slots occupied, the bridge created slots 1-4. Synthetic P1/P2/P3/P4 face reports appeared as A/B/X/Y on slots 1/2/3/4, respectively. Each player received 30 ViGEm updates; 120 packets were received with zero lost, rejected, or bad packets. All slots neutralized after release and were removed after bridge stop.

This closes the base PHI1/ViGEm/XInput question. It does not establish real four-device Android assignment, RetroArch port exposure, P3/P4 A8 profile/editor behavior, or four-player gameplay. The next diagnostic is physical Android-controller-to-XInput-slot assignment during a normal game session.

## Real Android four-controller assignment result

The observational normal-session probe returned `ANDROID_FOUR_CONTROLLER_ASSIGNMENT_CONFIRMED`. Four real controllers connected to the onn mapped cleanly to host XInput slots 1, 2, 3, and 4 in first-touch order. Each controller produced an isolated physical A (`0x1000`) only on its own slot; releases were confirmed, no simultaneous-A ambiguity was seen, four-slot continuity stayed true, and the final state was neutral.

This closes the Android `InputDevice -> player assignment -> PHI1 -> ViGEm/XInput` boundary for four devices. The next narrow question is RetroArch startup enumeration: do the four pre-existing XInput devices become RetroArch ports 1-4 without xinput-driver fallback? A diagnostic-only log probe is the correct next step; P3/P4 A8 schema/UI changes remain deferred until that result.

## RetroArch four-port runtime result

The fresh normal-session probe returned `RETROARCH_FOUR_PLAYER_PORTS_CONFIRMED`. Host XInput slots 1-4 were live; RetroArch selected the xinput joypad driver and logged Xbox 360 Controller configured in ports 1, 2, 3, and 4. XInput startup fallback was false. This closes the controller transport/device-enumeration chain through RetroArch.

## A8 P3/P4 extension decision

Source inspection shows the backend normalization, public profile serialization, capabilities response, runtime validation, and RetroArch bind generator already iterate `INPUT_PROFILE_PLAYERS`. The narrow production change is therefore to expand that tuple to four, generate analog-D-pad settings from the player count, and remove P1/P2 hard-coding in the Android profile UI. Schema stays at 1: legacy profiles with only P1/P2 are accepted and normalize missing P3/P4 maps to empty/default-autoconfig semantics. The Android UI derives its supported player list from companion capabilities and offers copy-from-any-other-player rather than a two-player-only sync assumption. Runtime validation remains required.

## A8 four-player profile/editor runtime result

The post-install probe returned `A8_FOUR_PLAYER_PROFILE_EDITOR_CONFIRMED`. The live companion stayed on schema 1 and reported `player1` through `player4`; all four inspected profiles exposed P1-P4 keys; a legacy P1/P2 profile normalized to four; the installed backend generated four analog-D-pad lines plus explicit Player 3 and Player 4 binds; and the probe did not modify user profile storage. On the onn, the user confirmed Player 1-4 edit actions and copy-from-another-player, then successfully synchronized custom inputs across all four players.

The four-player infrastructure and A8 layer are therefore closed as runtime validated. The next active hypothesis is regression-only: four persistent virtual devices and four-player profile capability must not disturb ordinary 1P gameplay/session teardown. After 1P passes, proceed to 2P and then representative 4P gameplay before A9.

## One-player regression result

The guided normal-session probe returned `PHASE_A_1P_REGRESSION_CONFIRMED` using Crash Bash (PS1). Four session XInput slots were present, but physical Player 1 A appeared only on slot 1; release was clean, the user reported normal gameplay and no takeover, RetroArch port 1 autoconfigured with xinput and no fallback, and normal End/Exit returned the companion inactive and removed all session slots.

This closes the 1P regression. Proceed to 2P before representative 4P gameplay and A9.

## Two-player regression result

The guided normal-session probe returned `PHASE_A_2P_REGRESSION_CONFIRMED` using Crash Bash (PS1). Four session XInput slots were present. Physical Player 1 A appeared only on slot 1 and Player 2 A only on slot 2; both released cleanly, gameplay was normal and independent, P3/P4 did not interfere, RetroArch ports 1/2 used xinput with no fallback, and normal End/Exit removed all session slots.

Proceed to representative real 4P gameplay before A9.

## Representative 4P gameplay probe

- Classification: `PHASE_A_4P_HOST_ROUTING_CONFIRMED_GAMEPLAY_NOT_CONFIRMED`.
- P1-P4 each reached exactly XInput slot 1-4; releases clean; no cross-control.
- RetroArch autoconfigured Xbox controllers on ports 1-4; no xinput fallback.
- Crash Bash Players 3/4 remained greyed out.
- No explicit Beetle PSX HW multitap setting found under `data/games/retroarch`.
- Next: exact core-options/storage audit; lower controller layers remain closed.

## PS1 multitap/core-options audit

- Active nightly `.opt` path discovered.
- Beetle PSX HW multitap port 1/2 both explicitly disabled.
- Current emulator manager contains no core-option session mechanism.
- Crash Bash live catalog response did not expose `max_players`.
- Next: exact post-A8 source-context audit before production patch.

## PS1 multitap exact source context and patch decision

The source-context audit returned `EXACT_POST_A8_SOURCE_CONTEXT_CAPTURED`. Exact current hashes were recorded for emulator manager, Games plugin, emulators config, and Android activity. The existing `controller_overrides.json` has 12 DualShock records and no Crash Bash entry; `set_controller_profile()` currently replaces a whole game record. The active Beetle core option files show both multitap ports disabled. Decision: preserve existing override entries, add generic `ps1_multitap`, generate a native content-specific `.opt` from current options, and configure Crash Bash for Port-1 multitap. Runtime validation is still required.

## Post-multitap regression and rollback decision

The first game-specific Beetle Port-1 multitap patch changed Crash Bash from P3/P4 greyed out to four human player slots. The immediate 4P runtime probe then failed lower in the physical-input path: P1 reached slot 1, while P2-P4 produced no XInput activity even though all four virtual XInput slots and RetroArch ports were present. The user observed that only 2-3 physical controllers stayed connected while PrivyHub/game streaming was active, whereas all four stayed connected when it was off, and confirmed the controllers were plugged in/charged. The patch is therefore development-only and causally suspect. Decision: exact rollback from its own backup before any further code changes, then repeat the known-good four-controller assignment probe during a normal game session.

## Rollback comparison and multitap re-enable

The exact rollback worked at the PS1 game layer: Crash Bash Players 3/4 were greyed out again. It did not restore the old four-controller Android result. P1-P3 were captured cleanly on slots 1-3 but P4 produced no input. Controllers were charged. Because the failure persists without multitap, re-enable the same game-specific multitap implementation and test with a different physical controller; keep Android controller stability as a separate active investigation.

## Final 4P acceptance and A9 transition

With a replacement physical controller, the Android four-controller assignment probe returned `ANDROID_FOUR_CONTROLLER_ASSIGNMENT_CONFIRMED` with exact P1-P4 -> slots 1-4. With game-specific Port-1 multitap re-enabled, Crash Bash Battle Mode then returned `PHASE_A_4P_GAMEPLAY_REGRESSION_CONFIRMED`: four human players exposed, four exact host routes, independent gameplay, no cross-control, xinput ports 1-4, and clean teardown. The earlier dropout is not classified as a software/multitap regression because it persisted once during exact rollback and did not reproduce with the replacement controller. Four-player work is closed; A9 is now active.

## 2026-09-10 A9 installer v1 preflight correction

`privyhub_phase_a_a9_regression_probe_01_2026-09-10` correctly returned **FAILED BEFORE MODIFICATION** before installing anything because its runtime-evidence preflight compared exact reconstructed chat-log bytes to the authoritative Windows log bytes. The visible successful measurements were unchanged; the gate was too strict about byte representation. A9 v2 replaces that check with semantic validation of the required classifications and raw measurements while leaving the A9 runtime probe itself unchanged.

## A9 first run and multitap UX decision

A9 returned NOT_CONFIRMED. Nearly all exercised behavior passed. NES and Genesis identification failed in the probe while the user's functional checks passed. CTR Battle showed P3/P4 greyed, revealing that the current multitap implementation is still only configured for Crash Bash. User set product scope to maximum four local players and requested a simple flag analogous to DualShock. Decision: On/Off only; On => Port 1 enabled, Port 2 disabled; checker recommendation from metadata, but no automatic enabling. Source/context + actual-library candidate audit is next.

## PS1 multitap flag audit result

The live candidate audit scanned 80 PS1 entries and returned zero candidates; both CTR and Crash Bash lacked usable `max_players > 2` metadata. Decision: drop Auto/recommendation from the current production change and add only a manual Multitap On/Off selector. Four-player ceiling means On maps exclusively to Port 1 and Port 2 remains disabled.

## Wireless ADB persistent build/install blocker

During the Multitap On/Off Android install, `tools/build_install_onn.ps1` failed at step 2 with no online ADB devices. The script's historical implementation performs one `adb devices` query and aborts. Because wireless debugging can remain paired while the connect service/ADB transport is temporarily absent, this is now tracked as a persistent tooling bug. A source/runtime ADB audit is the next diagnostic. No IP/network address will be requested or logged.

## ADB recovery audit v1 failed diagnostic

`probe_adb_wireless_recovery.py` raised `ADB could not be found using the same
sources as build_install_onn.ps1` and produced no log. Inspection found the
probe's `sdk.dir` regex had been double-escaped, so the failure was diagnostic
tooling rather than ADB evidence. A v2 probe replaces regex parsing with exact
property parsing and includes an escaped-Windows-path fixture.

## Persistent wireless ADB recovery decision

Restarting the PC ADB server did not restore discovery. The ONN still showed the
PC paired. The build/install script is being hardened with a private last-known
target cache plus bounded reconnect/mDNS/server-restart retries and one final
Wireless debugging Off/On retry. No re-pairing or address sharing is required.

## Wireless ADB recovery runtime success

After the persistent recovery patch, the audit returned
`ADB_TARGET_ALREADY_ONLINE`. The updated build tool had mDNS and reconnect logic
active; mDNS reported one TLS-connect service and ADB one online transport with
zero offline/unauthorized transports. Persistent ADB tooling blocker closed.

## CTR Multitap On/Off success

CTR runtime probe classified `PS1_MULTITAP_ONOFF_CTR_CONFIRMED`. Stored mode was
`port1`; the content-specific options file had Port 1 enabled and Port 2
disabled; four XInput slots were live; Players 3/4 were available; all four
controllers behaved independently. This closes the manual PS1 multitap feature.

The remaining Phase A work is A9 diagnostic closure. The first A9 full run
already passed every substantive stage except NES/Genesis identification, so a
corrected finish probe should rerun only those two families rather than repeat
the full test matrix.

## A9 NES/Genesis clarification

User clarified that no NES or Genesis games are currently installed. During the
A9 attempts the prompts were answered yes without actual NES/Genesis sessions.
Those answers are invalidated and neither family is runtime validated by A9.

Decision: make the A9 finish probe query current library counts before asking
runtime questions. Systems with zero entries are explicitly
`SKIPPED_NO_LOCAL_FIXTURE`; this remains a declared validation gap.

## A9 available-library completion and Phase A checkpoint

Coverage-aware finalizer measured NES library count 0 and Genesis library count
0, skipped both as `SKIPPED_NO_LOCAL_FIXTURE`, reused all legitimate prior A9
passing evidence, preserved CTR multitap success, and returned
`PHASE_A_A9_CHECKPOINT_READY_WITH_NO_FIXTURE_SKIPS`.

Repository audit at the gate:
- HEAD `25e9a1492a684dbaeebede90ea7ca4abd3eab1fb`;
- `git diff --check` clean;
- intended Phase A production/tooling changes remain local for one final
  checkpoint commit.

Adopted repository storage model: raw evidence stays local and ignored; major
checkpoint snapshots are sanitized/compressed and committed with hashes.

## Post-Phase-A roadmap v2

After pushing Phase A at `e65e8f89604ce325e6e3e0537d0287070b8996c5`, the roadmap was restructured around
new priorities:

1. diagnostics/self-test/support-bundle infrastructure before further complex
   architecture changes;
2. Sunshine/Moonlight cleanup after observability is available;
3. adaptive streaming driven by end-to-end LAN/Wi-Fi measurements for local
   sessions, with bitrate adaptation first and FEC/resolution changes later;
4. recursive local-first VOD poster art/metadata;
5. formal resource/Linux scaling;
6. optional OpenBIOS/open-platform portability work with compatibility fallback;
7. future smart-home, remote-PC-game and handheld-client expansion.

The current fixed game profile remains approximately 7 Mbps encoded video; the
8+1 FEC/protocol overhead explains a wire rate near 8 Mbps. This distinction
should remain explicit in diagnostics and adaptive-control design.
