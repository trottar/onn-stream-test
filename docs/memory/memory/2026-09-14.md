---
memory_schema: 1
as_of: 2026-09-14
baseline_commit: 45ef51f9e583b459752dd5ea83163f54171e68c7
---

# 2026-09-14

## C1.1 runtime validation

C1.1 static reference profile extraction passed representative runtime validation:
- profile/status identity matched `native_game_720p60_reference`;
- 1280x720 @ 60 fps;
- 7000 kbps target/max;
- GOP 15;
- B-frames 0;
- FEC group size 8;
- picture, process audio, controller input, Pause/Resume, Save/Load and End/teardown passed.

C1.1 was held uncheckpointed while the status privacy defect below was repaired.

## Native-stream status privacy defect

Review of post-session status found a network identifier and an absolute local
diagnostics path inside retained process-audio helper status. No sensitive values
are copied into durable memory.

The hotfix sanitizes only the public status boundary. Raw helper JSON remains local
for diagnostics. Status: LIVE ENDPOINT VALIDATED / CHECKPOINTED / PUSHED.

## C1.1 + native-status privacy checkpoint

**Status:** RUNTIME VALIDATED / CHECKPOINTED / PUSHED

This checkpoint combines the behavior-preserving C1.1 static reference-profile
extraction with the public native-stream status privacy hotfix.

Validated C1.1 runtime regression:
- picture;
- process audio;
- controller input;
- Pause/Resume;
- Save/Load;
- End/teardown.

Validated live public-status privacy boundary:
- network identifier findings: 0;
- absolute path findings: 0;
- unsafe keyed-value findings: 0;
- retained client field redacted;
- retained helper log path redacted;
- validator printed no sensitive values;
- raw helper status remained local/unmodified.

Next work is the remote-foundation architecture/roadmap documentation update
before deeper Phase C implementation.

## Remote-foundation architecture and roadmap promotion

The synchronized C1.1/privacy checkpoint is the clean predecessor for this
documentation-only architecture update.

Accepted correction:

- ordinary household router/Wi-Fi is upstream only;
- home Opal is the PrivyHub network/trust domain;
- future Linux hub and PrivyHub/IoT devices live behind the Opal;
- future travel GL-iNet-class router provides a portable trusted client LAN;
- secure overlay and PrivyHub application authorization are separate layers;
- Tailscale is the preferred first provider candidate, not a permanent
  dependency;
- source/request IP is not durable client identity;
- no permanent travel-router model is selected yet.

Phase C remains active. Its profiles, telemetry, adaptation/FEC behavior and
streaming boundaries must be reusable for future WAN work, while WAN overlay,
authentication and travel-router implementation remain deferred to Phase G.

Future WAN adaptation prioritizes interactivity: degrade quality before
queue/buffer growth creates runaway latency.

The current reference session is planned at roughly 10-11 Mbps outbound for
future WAN experiments once video, 8+1 parity, PCM16 stereo audio and
packet/tunnel overhead are considered. This does not change the stable PCM audio
path.

Roadmap order:

- G — Secure Remote Access / Portable Client Foundation;
- H — Extended Emulation & User-Content Import;
- I — Home Infrastructure / Broader Plugin Expansion;
- J — Local Intelligence / Voice / Privacy-Aware AI.

Decision: D-059.

Remote implementation status remains **PLANNED ONLY**.

## Remote-foundation architecture checkpoint

**Status:** CHECKPOINTED / PUSHED

The topology/roadmap promotion is checkpointed separately from the C1.1/privacy
production checkpoint.

Checkpointed architecture state:

- home Opal is the PrivyHub trust/network domain;
- ordinary household router/Wi-Fi is upstream only;
- Phase C remains active and must produce profiles/telemetry/adaptation
  boundaries reusable by future WAN work;
- WAN overlay/auth/travel-router implementation remains deferred to Phase G;
- Phase G is Secure Remote Access / Portable Client Foundation;
- later phases are H Extended Emulation, I Home Infrastructure and J Local
  Intelligence;
- Tailscale is the preferred first overlay candidate, not the permanent
  architecture contract;
- secure overlay transport and PrivyHub application authorization are separate;
- source/request address is not durable client identity;
- no permanent travel-router model is selected yet;
- deferred transport replay target is Linux + home Opal + onn;
- remote runtime implementation remains planned only.

Next development work returns to Phase C under D-059.

## Phase C2 telemetry inventory and minimal contract design

C2.1 source/contract inventory completed with:

`C2_INVENTORY_COMPLETE_EXISTING_FOUNDATION_WITH_GAPS`

The existing Phase-B feedback path already supplies receiver Mbps/FPS,
packet/loss/FEC counters, decoder queue state, stale/overflow drops,
rendered-frame continuity and receive/decode/output-gap timing on a 2-second
cadence.

The narrowed implementation gaps are:

- explicit RTP inter-arrival jitter;
- scoped control-path round trip;
- sender `sendto()` pressure/timing;
- signed decoder queue-depth change.

C2.2 design selects a companion-side measurement-only
`privyhub_stream_telemetry_v1` contract.

No second Android sampler is added. The existing client-health transport remains
`privyhub_client_health_v1` with only optional additive measurements.

No explicit starvation counter is required in the first patch; existing
render/FPS/output-gap/queue measurements are the raw basis for later C3
classification.

No bitrate/FEC adaptation is part of C2.2.

Decision: D-060.

Next:

`C2_IMPLEMENT_STREAM_TELEMETRY_V1`

## C2 telemetry design checkpoint

**Status:** CHECKPOINTED / PUSHED

C2.1 telemetry inventory and C2.2 minimal telemetry-contract design are now
checkpointed/pushed separately from production implementation.

Durable result:

- `C2_INVENTORY_COMPLETE_EXISTING_FOUNDATION_WITH_GAPS`;
- `C2_DESIGN_MINIMAL_STREAM_TELEMETRY_V1`;
- `privyhub_stream_telemetry_v1` is the adaptation-facing measurement contract;
- D-060 is accepted;
- implementation adds only receiver jitter, control-path round trip, signed
  queue-depth change and sender send-pressure/timing;
- no adaptive bitrate/FEC controller exists yet.

Next production step:

`C2_IMPLEMENT_STREAM_TELEMETRY_V1`

## C2 stream telemetry v1 development patch

Installed the D-060 measurement-only implementation: receiver jitter,
health-POST control round trip, signed queue-depth change, relay send-pressure
metrics, companion telemetry assembly and bounded runtime validator.

No adaptive controller or packet/wire behavior change.

Status remains **DEVELOPMENT PATCH / RUNTIME VALIDATION PENDING**.

## Wireless ADB stale-cache recovery regression

The C2 Android install exposed an untested D-053 failure branch: a stale private
cached `_adb-tls-connect._tcp` target caused `adb get-state` to emit
device-not-found, which PowerShell promoted to a terminating
`NativeCommandError` under script-wide `ErrorActionPreference=Stop`.

The existing recovery sequence was therefore aborted before connect/mDNS/
reconnect/server-restart recovery could run.

Development fix: route all exploratory ADB recovery commands through one
fail-soft wrapper that captures/suppresses native errors and returns explicit
exit/output state. Final recovered-device validation/install/launch remain
fail-hard.

The earlier D-054 `ADB_TARGET_ALREADY_ONLINE` result did not exercise this stale
cached-target branch. Runtime validation of this fix is pending.

C2 candidate source remains intentionally unchanged.

## C2 telemetry and stale-cache ADB runtime acceptance

C2 telemetry is runtime validated.

Automated result: `C2_STREAM_TELEMETRY_RUNTIME_PASS`.

Representative measurements:
- sample interval 2000 ms;
- receiver 7.4575 Mbps;
- receiver 61.3526 FPS;
- inter-arrival jitter 2.399 ms;
- packet loss delta 0;
- recovered FEC packets delta 0;
- unrecoverable FEC groups delta 0;
- control-path round trip 42 ms;
- receive-to-decode 30 ms;
- output gap 20 ms;
- decoder queue depth/delta 0/0;
- sender bytes delta 2,086,042;
- sender calls delta 1,929;
- sender errors delta 0;
- average/max sender call 33.189 us / 820.7 us.

Manual representative game regression also passed: picture, process audio,
controller input, Pause/Resume, Save/Load and End/teardown.

The stale cached wireless-ADB target regression is also runtime validated. In
the actual failure condition, the corrected build tool continued past the stale
cached target, recovered the physical onn via `cached-after-server-restart`,
installed the APK successfully and launched PrivyHub. The subsequent sanitized
audit showed one online transport, mDNS enabled, one TLS-connect service and no
offline/unauthorized transports.

The audit's old literal-source booleans for direct mDNS/reconnect invocation are
now stale because recovery commands go through `Invoke-AdbRecoveryCommand`.
Raw runtime behavior supersedes that heuristic.

No network address, endpoint, hostname, device serial or mDNS instance name is
recorded in durable memory.

Next after checkpoint/push: C3 adaptive bitrate.

## C3 source/policy audit

C3 began from clean synchronized checkpoint
`da03bb59cee9f7e9cf8bdfcc91dc1f52454beff0`.

Exact source review found C2 has the required measurement families, but the
current FFmpeg process has fixed startup bitrate/maxrate, uses `-nostdin`, and
directly consumes WGC stdout. The existing full stop path also stops stream
session I/O and FEC.

D-062 separates adaptation policy from a backend-neutral bitrate actuator.

The first C3 diagnostic keeps bitrate at 7000 kbps and tests actuator continuity
only. Lower production bitrate bounds remain unset pending characterization.

## C3 actuator continuity diagnostic implementation

Installed a companion-only development probe for D-062.

The probe exercises one unchanged-7000 video-only restart candidate:
- WGC capture restarts;
- FFmpeg restarts;
- FEC relay remains running;
- process audio remains running;
- persistent controller remains running;
- normal game lifecycle remains owned by the existing paths.

The trigger is loopback-only. The runtime tool reuses C2 telemetry and the
existing Android decoder-session report; no Android change or new sampler is
introduced.

Status: runtime evidence pending.

## C3 actuator runtime acceptance

The unchanged-7000 video-only restart probe completed with no classified
problems.

Receiver/decoder evidence showed one expected resync/SSRC transition, 17 ms
resync-to-IDR, zero packets dropped waiting for IDR, zero decoder drops, zero
queue-overflow drops, 2,131 rendered frames, and clean audio/controller error
counters.

Maximum decoder output gap was 791 ms and maximum receive-to-decode was 800 ms.
These values remain part of the evidence rather than being hidden by the
successful classification.

Focused play for several minutes felt normal. Possible slight stutter could not
be distinguished from existing occasional baseline stutter. No clear freeze,
black frame, audio interruption or controller stall was observed.

D-063 accepts the backend-neutral video-only restart strategy for initial C3.
Windows uses WGC + FFmpeg/NVENC; Linux must implement an equivalent backend-
specific actuator and rerun continuity validation.

Next: fixed-bitrate characterization.

## C3 fixed-bitrate characterization implementation

After checkpointing the accepted video-only actuator, C3 moved to one-level-at-
a-time bitrate characterization.

First candidate: 6000 kbps.

A companion-only loopback diagnostic switches the active video encoder from the
7000 reference to 6000 while preserving 720p60, GOP15, B-frames0, FEC8, process
audio, controller and game lifecycle. Native status now reports the actual active
encoder bitrate separately from the 7000 reference.

No Android change, production ladder, minimum bitrate or automatic controller is
introduced.

Runtime evidence pending.

## C3 6000 kbps runtime validation

The first lower fixed candidate, 6000 kbps, is accepted.

Gameplay/movement felt fine. Technical video evidence showed clean receiver
recovery with one decoder drop and one queue-overflow drop retained as raw
findings.

Audio was somewhat stuttery, but detailed receiver evidence showed alternating
queue overflow and starvation: 868 stale trims, 922 concealed underruns and 104
prolonged-starvation events, versus only 11 actual lost audio packets. This is
consistent with the separately deferred burst/gap transport pathology rather
than evidence that 6000 kbps is too high or too low.

D-064 records 6000 as a validated candidate and keeps audio transport replay
deferred to Linux + Home Opal.

Next candidate: 5000 kbps.

## C3 6000 acceptance installer warning-handling correction

The first 6000-kbps acceptance installer rolled back after `git diff --check`
printed line-ending warnings. The command had succeeded; the installer
incorrectly used non-empty output as its failure predicate.

The repository was restored exactly, so no production state was lost.

Process correction:
- exit code is authoritative;
- warnings are logged separately;
- `git diff --check` exit 0 + warnings must pass;
- package-side regression now tests success-with-warning and real failure.

The corrected v2 acceptance package includes this rule in durable memory.

## C3 fixed 5000 characterization implementation

After checkpointing D-064, C3 moved to the next single lower point: 5000 kbps.

The existing fixed-bitrate video-cycle implementation was generalized internally
rather than copied. Stable 6000 and 5000 wrappers call the same actuator body.

A new loopback-only 5000 Games action and two-stage runtime tool were added.
5000 keeps 720p60, GOP15, B-frames0 and FEC8 unchanged.

The final tool now prints the detailed audio queue/starvation metrics that were
needed manually during the 6000 review. Those counters are observational only;
the previously deferred audio burst/gap pathology is not used as a bitrate
failure without new causal evidence.

Runtime evidence pending.

## C3 5000 kbps runtime disposition

5000 kbps completed runtime characterization.

The run recovered quickly after the video-only restart and produced a
subjectively clearer image. After the initial lag period the game could feel
smooth, but extended focused play showed definitely more visual stutters than
the validated 6000/7000 settings.

The run also recorded 11 decoder drops and 11 queue-overflow drops.

D-065 therefore withholds ladder acceptance for 5000. The uncertain slight
input-lag impression is retained but is not required for the decision.

Audio burst/gap behavior remains separately deferred to Linux + Home Opal.

The lower usable boundary is now bracketed between 5000 and 6000 kbps.
Next candidate: 5500 kbps.

## C3 fixed 5500 characterization implementation

After checkpointing D-065, C3 moved to the midpoint of the 5000–6000 bracket:
5500 kbps.

The shared fixed-bitrate diagnostic was extended from supported targets
(6000, 5000) to (6000, 5000, 5500). Stable 6000/5000 wrappers remain in place
and one 5500 wrapper/loopback route/tool was added.

No Android, audio, controller, FEC, reference-profile or automatic-adaptation
implementation changed.

The 5500 final evidence tool includes detailed audio queue/starvation metrics,
but those remain observational under the separately deferred audio burst/gap
investigation.

Runtime evidence pending.

## C3 5500 kbps runtime validation

5500 kbps completed runtime characterization and is accepted as the current
Windows C3 lower ladder level.

Focused play showed the familiar short initial lag, recovery after a few
seconds, then very good gameplay.

The final decoder-session report contained 57 decoder drops and 57
queue-overflow drops. A follow-up inspection of the already captured C2 samples
showed six fresh post-cycle intervals from 5,502 to 15,536 ms with 638 rendered
frames and zero drop/overflow deltas, queue depth zero throughout.

The evidence does not localize the 57 whole-session drops outside that sampled
window, so they remain explicitly unresolved rather than being labeled as
startup-only.

D-066 freezes the Windows C3 controller ladder at 5500/6000/7000 and excludes
5000. Fixed-floor characterization stops here to avoid overfitting the current
test environment.

Next: adaptive bitrate controller implementation.

Audio burst/gap remains deferred to Linux + Home Opal. Linux migration later
revalidates actuator/fixed-envelope behavior.

## C3 startup stabilization implementation

D-067 adds a fail-closed native startup readiness gate before adaptive bitrate
work:
- stream start keeps emulator paused;
- Android readiness overlay + compact metadata;
- explicit ready/release action;
- controller input gating before release;
- shared metadata in existing paused Game Session UI;
- existing Back/Save/Load/End lifecycle preserved;
- automatic bitrate switching still disabled.

Runtime validation pending.

## D-067 runtime validation and stale-companion finding

Initial D-067 run showed the new Android GUI but `Release failed`. Existing
RetroArch lifecycle evidence proved the emulator really paused and later resumed.

Source comparison identified a mixed-version runtime: the new APK was installed
but the already-running companion still held predecessor Games plugin behavior.
That predecessor `native-stream-start` auto-resumed paused gameplay and did not
implement `native-stream-ready`.

Restarting the companion loaded the installed D-067 Python source. The next run
worked correctly and the user reported no startup lag.

D-068 accepts startup stabilization and makes companion restart after Python
changes a durable validation rule.

## C3 bidirectional actuator diagnostic implementation

After checkpointing D-067/D-068 startup stabilization, C3 moved to the final
actuator prerequisite before automatic bitrate policy.

D-069 adds one loopback-only `7000 -> 6000 -> 7000` diagnostic.

The existing shared fixed-bitrate video-only restart body is generalized rather
than copied. Existing characterization wrappers keep their original
reference-start behavior.

The runtime tool uses existing C2 telemetry, requires clean recovery after the
downshift before attempting the upshift, and finalizes against the normal
decoder-session report.

No Android, FEC, audio, controller, resolution/FPS or automatic adaptation
behavior changes. Runtime evidence pending.

## D-069 bidirectional actuator runtime result

The diagnostic completed `7000 -> 6000 -> 7000`.

Downshift:
- first RTP resume 952.789 ms;
- host verified 1,715.798 ms;
- sampled recovery clean.

Upshift:
- first RTP resume 837.313 ms;
- host verified 1,597.242 ms;
- first fresh interval had one decoder drop/overflow, then clean recovery.

Final decoder max output gap was 1,059 ms.

Focused play observed a roughly one-second freeze during one shift, then normal
recovery.

D-070 therefore accepts bidirectional functionality but rejects
`video_only_restart` as the automatic active-game actuator. Next investigate
live bitrate reconfiguration without encoder replacement.

## D-071 Linux-first phase reorder

After D-070, the project deliberately stopped short of an NVENC-specific live
bitrate-control investigation because the Windows server path is temporary.

The remaining roadmap was reordered so Linux comes before media polish:
- old E Linux Migration -> new D;
- old F Linux Characterization -> new E;
- old D Media/VOD/Live TV -> new F;
- G onward unchanged.

Linux migration preserves the currently working media baseline but does not wait
for VOD/EPG polish.

Automatic bitrate/FEC continuation and deferred transport replay move to the
representative Linux path. Windows bitrate levels remain evidence rather than
Linux constants.
