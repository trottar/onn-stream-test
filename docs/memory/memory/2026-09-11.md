# 2026-09-11

## B4.6 v1 packaging defect

The first physical-cleanup installer was rejected before modification because
B4.6 v1 hashed literal `\\0` / `\\n` byte sequences while B4.5 used real
NUL/newline canonical separators.

Authoritative project files were not modified.

B4.6 v2 corrects the canonicalization and adds a direct equivalence regression
against the B4.5 implementation.

## B4.6 v2 packaging defect

B4.6 v2 reached the discovery gate and falsely classified the nine
B4.5-authorized RetroArch Moonlight-name collisions as unexpected targets.

The cause was over-escaped literal-dot regexes in the v2 manifest helper.
The installer failed before modification.

B4.6 v3 copies B4.5 discovery semantics exactly and adds direct discovery
equivalence fixtures plus an exact non-target preservation allowlist.

## B4.6 runtime result

Project-side physical legacy cleanup is confirmed with native streaming active
and all intended RetroArch non-targets preserved.

B4.6 could not verify Android package state because its probe used PATH-only ADB
discovery.

## B4.7

Use established private ADB discovery/recovery to check `com.limelight` without
logging identifiers or addresses.

## B4.7 result

Private ADB recovery confirmed `com.limelight` remains installed on the paired
onn while PrivyHub is present.

## B4.8

Perform a package-only uninstall of `com.limelight`, then verify PrivyHub
remains and Moonlight is absent before B5.

## B4.8 first removal result

The device action successfully issued the Moonlight uninstall and preserved
PrivyHub. Follow-up state showed Moonlight absent, but the action classified
failure because `pm path` returned nonzero for the missing package.

This is a verifier bug, not evidence that uninstall failed.

A corrected final-state verifier uses package-list semantics and performs no
further uninstall.

## B4 complete

Corrected B4.8 final verification confirms PrivyHub present and Moonlight absent
on the paired onn. B4 legacy removal is complete.

## B5 started

Proceed with one focused native-only regression session. The harness should
capture objective native-stream/save/teardown evidence and explicit user
confirmation of visible/audible/controller and profile behavior.

## B5 adjudication

The first B5 run reported only `post_profile_native_ok` false. All functional
checks passed.

The fresh game diagnostic bundle showed a continuous ~176.5 s Android decoder
session with zero video packet loss, zero decoder drops, zero unrecoverable FEC
groups, and complete frame accounting. The client session ended about ten
seconds before normal game teardown, matching the B5 post-profile polling
window.

This established a harness acceptance error rather than a streaming regression.
B5 is runtime validated; B6 is next.

## B6 started

B5 is runtime validated. Begin B6 with a read-only repository audit. Capture the
exact status list and compile/build readiness before constructing the final
checkpoint/push package. Root ROADMAP.md is intentionally not changed until the
audit result is reviewed.

## B6 audit v1 packaging defect

The first B6 repository-audit installer rejected the current working
`docs/ROADMAP.md` before modification because it required byte identity with a copied
baseline.

This was a diagnostic-package design error. B6 v2 audits the working roadmap
through Git state and preserves its current bytes without requiring external
byte identity.

## B6 final checkpoint

The B6 audit showed zero unexpected/pre-staged paths, clean diff checking,
required evidence present, raw evidence ignored, and passing Python/Kotlin
compilation.

The two readiness failures were audit-model defects: one exact known-inert
legacy text hit and a recursive manifest self-entry. The finalizer corrects both,
updates `docs/ROADMAP.md`, selectively stages only audited/checkpoint paths,
commits, pushes, verifies remote main, and advances durable state to C1.


## Roadmap v3 merged and accepted

After the Phase B checkpoint, the Linux-first continuation was merged into the
main roadmap and accepted before Phase C implementation.

Changes:
- retained C adaptive-streaming sequence as next;
- expanded D to include substantial Live TV channel/EPG/guide repair;
- dedicated E to Linux migration on the HP EliteDesk 805 G6;
- dedicated F to optimized Linux resource characterization with PS1-and-below;
- made G the user-content import plus N64/GameCube/PS2 feasibility phase;
- moved home/client/plugin expansion to H;
- made I the local-first intelligence/voice and optional external-AI phase;
- retired OpenBIOS as a project requirement;
- formalized user-supplied ROM/ISO/BIOS/firmware import;
- formalized privacy-aware optional OpenAI/Anthropic/other provider integration.

No production/runtime behavior changed.


## C1.1 started

Phase C began with a diagnostic-only exact inventory of the current validated
stream configuration and ownership boundaries. The inventory precedes any
profile-schema or runtime behavior change.

## Docs + memory cleanup Part 1 repair — 2026-09-11

Part 1 current-state cleanup was repaired after a delivery-process regression. Repair 01 then safely rolled back because LF/CRLF advisory warning text from `git diff --check` was incorrectly treated as a failure despite Git returning success.

Authoritative evidence before repair:
- Git HEAD: `a6dbf627dd32af7da975bf01a679350810026ca3`;
- no staged files;
- `MEMORY.md`, `investigations/ACTIVE.md`, and the C1 inventory probe remained
  present locally and uncommitted;
- all six Part 1 tracked files exactly matched HEAD;
- the manifest registry pointed at the local uncommitted C1 `MEMORY.md` and
  `ACTIVE.md` bytes rather than their committed HEAD bytes.

The earlier cleanup installer rolled back on deterministic validation, then a
separate checkpoint command accidentally committed the pre-existing append-only
C1 memory surfaces instead of the intended concise replacements.

Repair rules:
- preserve the three local C1 files byte-for-byte;
- replace only `CURRENT.md`, `CURRENT_HANDOFF.md`, and roadmap `STATUS.md` with
  concise present-tense state;
- append this dated repair record and the patch-index record;
- transform `manifest.json` against the exact local tree and fully validate its
  registry;
- perform no staging, commit, or push in the installer step.

No production/runtime behavior changes.

## Part 1 + C1 inventory checkpoint finalization

The exact checkpoint preflight returned `PART1_CHECKPOINT_READY`.

Checkpoint scope is deliberately the nine validated local paths:
- six Part 1 repair/current-state files;
- `MEMORY.md`;
- `investigations/ACTIVE.md`;
- `tools/probe_c1_stream_parameter_inventory.py`.

This scope is required because the durable-memory manifest correctly references
the current local C1 `MEMORY.md` and `ACTIVE.md` bytes. Staging only the six Part
1 files would recreate a Git/manifest inconsistency.

The checkpoint finalizer:
- exact-hash gates all nine paths;
- preserves the C1 files byte-for-byte;
- advances current/handoff wording from checkpoint-pending to checkpointed;
- rebuilds and validates the complete durable-memory manifest;
- stages only the nine reviewed paths;
- commits and pushes only after all local validation passes;
- verifies `origin/main` equals the resulting local HEAD.

Part 2 docs/memory curation is next. No production/runtime behavior changes.

## Part 2 durable-memory curation

Part 2 converts live durable memory from append-heavy chronology to explicit
current-state roles without deleting historical context.

Changes:
- `MEMORY.md` is replaced by a curated present-tense durable fact/rule set;
- `AGENTS.md` is updated from the obsolete pre-four-player guardrail to the
  current Phase C / docs-cleanup operating contract;
- `history/README.md` defines the deep-memory trust order;
- `history/MEMORY_SUPERSEDED_THROUGH_2026-09-11.md` is created from the exact
  pre-Part-2 `MEMORY.md` bytes.

Detailed evidence, dated history, decisions, investigations and patch history
remain in their existing dedicated locations.

Part 2 changes no production/runtime behavior. Installation and checkpoint/push
remain separate steps.

## Part 2 checkpoint

The Part 2 checkpoint preflight returned `PART2_CHECKPOINT_READY`.

Checkpoint scope is the exact nine validated Part 2 paths:
- curated `MEMORY.md`;
- current `AGENTS.md`;
- `CURRENT.md` / `CURRENT_HANDOFF.md`;
- full durable-memory manifest;
- dated memory and patch index;
- `history/README.md`;
- exact superseded `MEMORY.md` snapshot.

The superseded snapshot remains byte-identical to pre-Part-2 `MEMORY.md`.

Part 2 is now checkpointed/pushed. Part 3 decisions/investigations normalization
is next. No production/runtime behavior changed.

## Part 3 decisions/investigations normalization

The Part 3 preflight returned `PART3_NORMALIZATION_PREFLIGHT_READY`.

Measured state before normalization:
- six reused decision IDs across eleven extra ID occurrences;
- 45 sections in `investigations/ACTIVE.md`, most belonging to completed
  Phase A/Phase B work;
- only four sections in `CLOSED.md`;
- stale roadmap-era wording in deferred UDP/bootstrap entries.

Normalization:
- retain the first durable occurrence of existing decision IDs;
- assign D-047 through D-057 to the eleven colliding detail sections;
- add explicit supersession/current-status notes to those renumbered sections;
- preserve the exact pre-Part-3 `ACTIVE.md` bytes in deep memory;
- replace live `ACTIVE.md` with only current docs-cleanup and queued C1 work;
- append a closure summary to `CLOSED.md`;
- update deferred wording to roadmap v3 without reactivating deferred work;
- leave production/runtime source and Part 4 top-level docs untouched.

Installation and checkpoint/push remain separate steps.

## Part 3 checkpoint

The Part 3 checkpoint preflight returned `PART3_CHECKPOINT_READY`.

Checkpoint scope is the exact ten validated Part 3 paths:
- normalized decision log with unique IDs through D-057;
- current-only `investigations/ACTIVE.md`;
- expanded `CLOSED.md`;
- roadmap-v3-normalized `DEFERRED.md`;
- exact superseded ACTIVE deep-memory snapshot;
- current/handoff/manifest/date/patch memory.

The predecessor ACTIVE snapshot remains byte-identical to its pre-Part-3 hash.
Part 3 is checkpointed/pushed. Part 4 top-level docs/ledgers refresh is next.

No production/runtime behavior changed.
