---
memory_schema: 1
as_of: 2026-09-17
---

# 2026-09-17

<!-- PRIVYHUB_D117_PULL_VALIDATION:DAILY:BEGIN -->
## D-116 accepted; D-117 pull validation next

D-116 runtime classification:

`D116_ANDROID_TV_STATE_SYNC_RUNTIME_VALIDATED`

Linux and onn both reached revision 2 with identical canonical durable-state
hashes. The known-bad 10 Bold stream was manually hidden on both sides.

D-117 is a reversible diagnostic for the remaining Linux-to-onn direction. It
changes only `country_name`, never `country_code`, restores original Linux
user-state content, and then verifies restored local/remote parity.
<!-- PRIVYHUB_D117_PULL_VALIDATION:DAILY:END -->

## D-115 runtime acceptance

D-115 Linux TV-state authority passed its real HTTP runtime probe.

Classification:

`D115_TV_STATE_AUTHORITY_RUNTIME_VALIDATED`

Validated:
- persistent revisioned state;
- idempotence;
- stale-write conflict protection;
- runtime-field exclusion;
- exact probe cleanup/restoration.

## D-116 Android TV-state synchronization

D-116 connects Android to the validated authority.

Initial one-client behavior:
- seed uninitialized Linux from onn durable state;
- pull initialized Linux state on TV entry;
- push durable local mutations with optimistic revision checking;
- fail soft when Linux is unavailable;
- do not sync runtime health/recency/EPG/catalog state.

Runtime test will use manual Hide on the known-bad 10 Bold source as the
post-seed mutation.
