---
memory_schema: 1
as_of: 2026-09-23
---

# 2026-09-23

## N05 closes the link-drop set (00:08 local)

The user re-ran N05 with the companion listening. Fault 0.556 s; no
`desync_pause` (the state read `PLAYING` throughout); the receiver resynced
on its own (one sequence resync of 423 packets, no restart, no SSRC
change); `max_output_gap_ms` 625. **PASS → `R3` + `R3a` RUNTIME VALIDATED
for real loss** (N05, N3, N15, N15b, N150). Appended to
`evidence/D_BASE_R3D_HAND_RUNS_2026-09-22.md`.

## `D-BASE-R3c2` — recovery resumes into a fresh, running core. RUNTIME VALIDATED

Record: `evidence/D_BASE_R3C2_RECOVERY_FLOW_2026-09-23.md`; patch
`patches/D-BASE-R3C2_RECOVERY_FLOW.md`. The user's rule plus option A,
implemented in the companion (`load_recovery_state_running`, tile fields,
cross-title stop, new `recovery-resume` / `recovery-copy`) and the client
(tile Resume / prompt-before-launch; no poll-driven prompt). APK
`21e3d089…9dcb`. Seven checks, all PASS: the N150 save now plays (607
distinct in 30 s; 61/61 distinct IDR sizes through the client), the Resume
tile reuses the live pid with no load, the prompt appears only with no
live session, discard / copy / cross-title as decided, and a gameplay save
through the same path plays at 60 fps (1,630 distinct). Harness snag: the
NOW PLAYING bar also shows the bare title, so the first check-1 attempt
tapped the bar; tile text is now matched in full.

## `H3` — companion autostart designed, not installed

Record: `evidence/H3_COMPANION_AUTOSTART_DESIGN_2026-09-23.md`. Facts: the
user manager holds `DISPLAY=:0` after autologin, `graphical-session.target`
is never activated (XFCE), `default.target` is; `Linger=no` is fine with
autologin. The companion binds 0.0.0.0, finds the capture window per
stream start, and **shuts down cleanly only on SIGINT** — SIGTERM (a plain
`kill`) skips its `finally` and orphans a live game; stdout is
block-buffered when redirected. Route A chosen (user unit,
`WantedBy=default.target`, bounded wait for X, `KillSignal=SIGINT`,
`python3 -u`, `Restart=on-failure`); XFCE autostart recorded as the
alternative. Nothing installed; install/undo steps and `h3_verify.sh` for
the user.

## `D-BASE-P9` — the audio cushion, measured; default reverted by the rule

Record: `evidence/D_BASE_P9_AUDIO_CUSHION_2026-09-23.md`; patch
`patches/D-BASE-P9_AUDIO_CUSHION_SETTING.md`; decision
`decisions/D-BASE-P9_AUDIO_CUSHION.md`. The user installed `H3` first
(user-stated: verified across a reboot); every restart today went through
`systemctl --user restart privyhub-companion`.

Before any session the code showed the handoff's premise off by one
field: the queue **target** is only the startup prefill, the running depth
sits at the **capacity** (P8: residence 30.9 ms against 40 ms; trims =
concealed underruns). 12 / 24 would cost ~+80 ms. The user chose to run
both 12 / 17 (+45 ms) and 12 / 24 beside a fresh 3 / 8.

Built the setting (profile fields, env override, `audio_cushion` in the
status/start response, client queue at 32 with the capacity enforced),
APK `a9355bb0…4a72`, device hash equal. Smoke: the host's values land
after the first PCM, so the prefill stays at 3 — the `P2a` hold does not
move. Sessions (20 min each, 0 adb in every hold): starvation/min 42.4 →
**0.8** (12 / 17) → 0.1 (12 / 24); residence **+36.0** / +71.7 ms; holes
unchanged; video unchanged; **underruns 20 → 25 → 16**. The pre-registered
rule reverts on any underrun rise, so the profile default is **3 / 8
again** and B is FALSIFIED by the rule; re-adoption is the user's call.
Audio `lost_packets` rose across the hour (282 → 990 → 1,380, video
flat) — not the queue (counted before it); confounded with arm order.

## `D-BASE-P9a` — the cushion interleaved against noise; 3/8 kept, adoption open

Record: `evidence/D_BASE_P9A_CUSHION_INTERLEAVED_2026-09-23.md`. No code
change; `P9`'s harness reused. A3 / B2 / A4 / B3 (3/8, 12/17, 3/8,
12/17), 07:53-09:17 UTC, 0 adb in every hold. 12/17: starvation 0.75 /
0.20 per min against 41.4 / 46.3, residence +37.9 / +33.5 ms, underruns
12 / 19 inside the 3/8 band of 4-20 (six sessions, largest event 3).
The rule as coded failed only on holes: B3 -22.5 % against A4, which was
itself +28 % over A3 and every earlier 3/8 session. Asked, the user kept
3/8 and will decide. Audio loss 442 → 1,151 → 1,417 → 1,676 in run order:
time-driven (builds over back-to-back streaming, reset by the idle gap
after `P9`), not the cushion's.

## `D-BASE-T2` — 12/17 adopted; the warm-state audio loss, reproduced, not located

Record: `evidence/D_BASE_T2_STREAMING_ACCUMULATION_2026-09-23.md`.
Part 0: the user adopted 12/17 ("No issues from playing for a minute or
two", user-stated); profile default set, the manager's override from their
listen unset, restarted through systemd — 12/17 `profile`, no env, no
client build. Patch `patches/D-BASE-T2_P0_AUDIO_CUSHION_ADOPTED.md`.

Part 1, 15:15-17:42 UTC: a new 10 s sampler (host, onn, Opal) across
S-a (cold, 42 min after the user's listen), S-b, 30 min idle, S-c, S-d.
Audio loss openings 4.3 / 46.0 / 4.0 / 68.3 per min: reproduced and reset
by idle. From cold it steps up at minute 7-8. Pre-registered reading
MIXED (host NVMe and Opal SoC temperatures |rho| 0.61-0.64, every
temperature reset). Exploratory: slow temperatures separate the step, the
fast host Tctl does not (it had cooled before S-b, which opened high);
video loss and the radio flat. Found on the way: the onn's live CPU
temperature via `dumpsys thermalservice` (62-70 °C streaming, status 0).
Thresholds proposed only; next probe named, not run.

## `D-BASE-T3` — the warm-state audio loss is lost between the ends

Record: `evidence/D_BASE_T3_AUDIO_LOSS_LOCATION_2026-09-23.md`. The code
first: the host sender advances the RTP sequence on a failed send too, so
`send_errors` is the host's whole share — and `T2`'s four sessions had 0.
One cold session (65 min idle), 18:46-19:11 UTC, 12/17 from the profile,
with `P5`'s onn socket sampler and a new host sampler at 2 s, an onn
snmp/wlan0 sampler (added during the run, before PLAYING) and `T2`'s
thermal sampler at 10 s. The step reproduced (rise at minute 7, 30/min at
11, onn ~68 °C). After it: client lost 649; onn audio-socket drops 0, onn
`RcvbufErrors` 0, host send errors 0, host `SndbufErrors` 0, sender at
200/s exact. Reading AIR. No code changed; no counter needed.

## `D-BASE-P10` — audio sent twice; adopted

Record: `evidence/D_BASE_P10_AUDIO_REDUNDANCY_2026-09-23.md`; patch
`patches/D-BASE-P10_AUDIO_REDUNDANCY.md`; decision of the same name.
Companion sends each audio datagram again 4 ticks later; client
de-duplicates first and fills concealment slots with late copies (and no
longer resets the sequence backwards on a late packet — a latent bug).
APK `f31b1c18…8ae7`. Warm, interleaved A1/B1/A2/B2 after a 22-min warm-up
(the onn read 63.7-65.5 °C at each start after the inter-hold gap, 67.3-68.1
mean in each hold): audio loss 78.5 / 2.8 / 77.5 / 1.4 per min, recovered
by copy 820 / 772, crossfades in step, residence and underruns unchanged,
+1.6 Mbps. The rule failed only on video FEC recoveries being *lower* with
it on; asked, the user adopted 2/4 as the profile default.

## `D-BASE` close-out — BASELINE MET; Phase C resumes

Record: `evidence/D_BASE_CLOSEOUT_2026-09-23.md`. First, the user's listen
with redundancy on — "sound good" — recorded in the `P10` record and
decision. The checkpoint commit (`824c9d9`) was the user's. No code or
profile change: cold session C (41 min after the listen) and warm W, 20
min each, 22:31-23:12 UTC, the adopted profile. Every numeric row passes in
both (spikes 32.8 / 26.0, fps 59.90 / 59.91, stale 1.1 / 0.8, video loss
8.7 / 8.4, underruns 17 / 14); max output gap 163 / 110 ms inside the 250
bound. Verdict BASELINE MET; client decision not triggered; `D-BASE`
closed, Phase C resumed at `C3.L3a` Part 2. CURRENT trimmed to Phase C
(10 → 5 KB); `D-BASE` conclusions curated in `MEMORY.md`; handoff
rewritten.
