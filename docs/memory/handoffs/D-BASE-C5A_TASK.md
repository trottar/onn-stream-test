---
memory_schema: 1
as_of: 2026-09-20
status: TASK HANDOFF — C5a, the IDR-rejection counter probe from the C5 read; diagnostic counters only, no behaviour change; sending this task to the host session is the user's authorization; superseded once its evidence record exists
---

# C5a task handoff — count the rejected IDRs

Implement the probe from docs/memory/investigations/C5_RESYNC_IDR_ACCEPTANCE_READ_2026-09-20.md §4 in /home/privyhub/Projects/onn-stream-test. Run only after `D-BASE-P2a` is recorded in docs/memory/CURRENT.md. Unattended, ask nothing. Read first: the C5 note (all of it), docs/memory/TOOLS.md, PrivyHub/app/src/main/java/streaming/RtpH264Receiver.kt (the marker-bit judgement around the completeness gate, `beginStreamResync`, `completeStreamResync`, the `FirstIdrAfterDiscontinuity` row).

**The change, receiver counters only — the receiver's behaviour does not change by one byte of output.** Two session counters exported in the report under `video`: `idr_aus_rejected_waiting_for_idr` (an access unit that contained NAL type 5 and was discarded while `waitingForIdr` because `currentCorrupt` was true) and `non_idr_aus_dropped_waiting_for_idr` (AUs without type 5 discarded while `waitingForIdr`). Note that today the completeness check runs before the IDR check, so a corrupt AU is discarded without knowing whether it was an IDR: the probe must inspect `containsNalType(frame, 5)` on the corrupt AU's bytes for counting only, and must not change which AUs are delivered. Also add `rejected_idr_aus` and `dropped_non_idr_aus` per discontinuity to the `FirstIdrAfterDiscontinuity` row. Build, install per TOOLS.md, companion restarted with 8765 checked free, `git diff --check` clean.

**Validation.** Resyncs are needed and the link is quiet tonight (P1 and P2 saw zero discontinuities in 18 sessions), so do not rely on natural ones. Without root, use the `D-BASE-R3` SIGSTOP substitute in its short form only: three sessions of the PS1 reference title, 150 s each, and in each one apply SIGSTOP/SIGCONT to the encoder for **0.3 s** at 30 s, 60 s, 90 s and 120 s (below `DESYNC_MS`, so recovery does not pause the game — confirm `desync_pause` is absent from the recovery log; if it fires, shorten to 0.2 s and re-run that session). The R3a record showed these produce real sequence jumps (720 / 984 / 480 packets) after SIGCONT. That gives up to twelve discontinuities. Add every natural discontinuity that occurs.

Per discontinuity report: `type`, `jump_packets`, `resync_to_idr_ms`, `rejected_idr_aus`, `dropped_non_idr_aus`, `au_complete`. Then the decision the C5 note pre-registered: the hypothesis is **confirmed** if every discontinuity with `resync_to_idr_ms` > 250 carries `rejected_idr_aus` ≥ 1 and those ≤ 250 carry 0; **falsified** if discontinuities > 250 ms show 0 rejected IDRs; **INDETERMINATE** if fewer than 6 discontinuities were obtained or none exceeded 250 ms (say so; the counters stay in for the corpus to accumulate). Do not implement the bounded fallback; it is a separate decision.

Record raw numbers first in docs/memory/evidence/C5A_IDR_REJECTION_COUNT_<date>.md with reports and recovery-log lines under evidence/c5a_<date>/ and SHA-256s; patch record in docs/memory/patches/ and PATCH_INDEX.md; update CURRENT.md (fixed headings, `python3 tools/check_memory_health.py` healthy), MEMORY.md, handoffs/CURRENT_HANDOFF.md, investigations/ACTIVE.md, the dated memory file, the C5 note's status line, and the battery file's C5 status. Teardown per TOOLS.md, no process left in state T. Never retry a failing action more than twice. No IP addresses, ADB endpoints or device identifiers in any memory or evidence file.
