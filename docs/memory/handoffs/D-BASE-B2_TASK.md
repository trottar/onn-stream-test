---
memory_schema: 1
as_of: 2026-09-21
status: TASK HANDOFF — Group B item B2 as corrected 2026-09-21: the host moved from the Windows PC to the Opal, the production topology; measurement only, no code change; runs after the user has moved the cable
---

# B2 task handoff — the host wired directly into the Opal

**Correction that motivates this** (user, 2026-09-21): the host's Ethernet has been running to the **Windows PC**, not the Opal, and the whole corpus was measured on the path host -> Windows PC -> Opal -> onn. The Windows PC is the machine whose UDP burst/gap/duplication pathology was measured in the Windows era and deferred. This test moves the host onto the production topology — host wired directly into the Opal, onn wireless, one hop — and measures whether the burst loss follows the PC or stays. No code change. Read first: docs/memory/CURRENT.md, docs/memory/TOOLS.md, docs/memory/evidence/GROUP_A_DIAGNOSTIC_AUDIT_2026-09-20.md (A2.2 and the A4 correction at the end), docs/memory/investigations/BASELINE_STREAM_HEALTH.md (Step 3), docs/memory/investigations/SEAMLESS_LOCAL_PLAY_TEST_BATTERY.md (Group B).

**Gate — confirm the topology before measuring anything.** Do not proceed on the user's word alone; check on the host: the default route's interface, the gateway's identity as far as it can be read without recording addresses (hostname / vendor prefix class, not the MAC or IP itself), whether the host and the onn are on the same subnet, and that `adb` reaches the onn. Record only "gateway is the Opal" / "gateway is the PC" and the interface name. If the host still routes to the onn through the PC, stop and say so — INDETERMINATE, no sessions. Also record how the host reaches the internet in this topology (the Opal's uplink, or a second interface), as a qualitative statement.

**Sessions, both arms on the same night.** The reference is tonight's corpus on the PC path (`D-BASE-P1` A60/B60/C60, `D-BASE-P2a` Q1-Q5, `C5a` R1-R3 — all on the same build, all attract mode). New arm: **six** attract-mode sessions of the PS1 reference title, 120 s each, zero input, per TOOLS.md, BACK to end, teardown between, on the Opal path. If the user has the PC path still available afterwards, do not switch back yourself; the reference arm is the existing reports.

**Per session report:** `lost_packets`, `lost_packets_in_resyncs`, loss per minute, forward-gap events, packets per gap event (mean and max), `max_forward_gap_packets`, `late_or_reordered_packets`, discontinuities with type and jump, `max_output_gap_ms`, `spike_20_ms` per minute, rendered fps, `audio.lost_packets`, `audio.underruns`, and every C5a counter and per-discontinuity field. Then the two arms side by side: medians, and for loss per minute and packets-per-gap the full per-session lists, because the day-to-day swing (31-392/min in Group A) is larger than most effects.

**Pre-registered reading.** The Windows PC is implicated if, on the Opal path, loss per minute falls by an order of magnitude *and* the per-gap burst size collapses toward ~1 (uniform loss) or forward-gap events largely vanish; it is exonerated if loss per minute and the ~14-packet burst shape are unchanged within the day-to-day spread; anything in between is reported as such with the numbers. Do not claim a verdict on six sessions if the two arms overlap. Also report whether the `D-BASE-R3a`-style resume-burst jumps are reproducible here (one session with a single 3 s SIGSTOP/SIGCONT at 60 s, recovery will pause and resume; note the discontinuity and its size) — this is the burst-then-drop mechanism suspected of living in a receive buffer, and if it disappears without the PC in the path that is worth knowing.

Record raw numbers first in docs/memory/evidence/B2_HOST_ON_OPAL_<date>.md with reports under evidence/b2_<date>/ and SHA-256s; classify CHARACTERIZED / INDETERMINATE. Update CURRENT.md (fixed headings, `python3 tools/check_memory_health.py` healthy — trim; the file is at its limit), MEMORY.md (the topology fact: production path is now measured), handoffs/CURRENT_HANDOFF.md, investigations/ACTIVE.md, the dated memory file, BASELINE_STREAM_HEALTH.md Step 3 and the target table's loss row, the battery file's B2 status, KNOWN_ISSUES.md (the UDP burst pathology entry: state which side of the PC it lives on). Teardown per TOOLS.md. Never retry a failing action more than twice. No IP addresses, MACs, SSIDs, ADB endpoints or device identifiers in any memory or evidence file — topology is described by role only.
