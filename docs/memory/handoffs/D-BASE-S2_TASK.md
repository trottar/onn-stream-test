---
memory_schema: 1
as_of: 2026-09-21
status: TASK HANDOFF — D-BASE-S2, one uninterrupted 3-hour session on the PC path; answers S1's RetroArch memory question, feeds C5a and the starvation series; measurement only; authorized 2026-09-21 for an unattended overnight run
---

# D-BASE-S2 task handoff — the long single session

Run only after `D-BASE-S1` is recorded in docs/memory/CURRENT.md (it is). Unattended, ask nothing. Read first: docs/memory/evidence/D_BASE_S1_OVERNIGHT_SOAK_2026-09-21.md (its sampling method, the +108 MB RetroArch growth per 30 min, the starvation rate, the per-minute heartbeat series), docs/memory/CURRENT.md, docs/memory/TOOLS.md, C5A_IDR_REJECTION_COUNT_2026-09-21.md (five natural discontinuities so far, floor of six).

**One attract-mode session of the PS1 reference title, 3 hours (10,800 s), zero input**, per TOOLS.md, BACK to end, then full teardown. Recovery is live; if a natural outage pauses the game, let it recover; a give-up or `END_MS` is a result — record it and end cleanly. Reuse S1's host-side sampler unchanged (every 30 s: temperatures, load, per-process CPU / RSS / threads for RetroArch, companion, encoder; FEC status; `status` recovery block; log sizes) — note S1's `comp_rss_kb` defect and make sure the companion is sampled correctly this time. Also sample RetroArch's `/proc/<pid>/status` VmRSS, RssAnon, RssFile and VmSwap so anonymous growth can be told from file-backed cache.

**Questions:** (1) does RetroArch RSS plateau, keep growing linearly (+108 MB per 30 min would be ~650 MB at 3 h), or accelerate — the per-30-s series with a piecewise reading, and whether the growth is anonymous or file-backed; state the host's total RAM and how long that rate would take to exhaust it; (2) does the companion's RSS plateau; (3) `prolonged_starvation_events` over 3 h — still linear at ~75/min, and does `avg_queue_residence_ms` change; (4) any stream metric drift over 3 h — per-minute rendered fps and output age from the heartbeat, Spearman against elapsed minute; (5) natural discontinuities with every C5a field — if the corpus of natural discontinuities on the C5a build now reaches six, apply C5a's pre-registered rule and record the verdict in the C5a record's status line; (6) did the heartbeat log rotate (it should not reach 4 MiB in 3 h; say the size it reached).

Record raw numbers first in docs/memory/evidence/D_BASE_S2_THREE_HOUR_SESSION_<date>.md with the report, heartbeat and recovery logs and the sampling file under evidence/d_base_s2_<date>/ with SHA-256s; classify CHARACTERIZED / INDETERMINATE. Update CURRENT.md (fixed headings, `python3 tools/check_memory_health.py` healthy — trim, it is at its limit), MEMORY.md, handoffs/CURRENT_HANDOFF.md (the host is still on the PC path; `D-BASE-B2_TASK.md` is next once the cable moves; `D-BASE-R3b`'s harness `evidence/d_base_r3b_2026-09-21/r3b_run.sh` is ready for the user to run by hand), investigations/ACTIVE.md, the dated memory file, KNOWN_ISSUES.md (a RetroArch memory-growth entry if it does not plateau). Teardown per TOOLS.md: no process in state T, no listener on 8765 / 48100-48102 / 48110, no banner on the onn. Never retry a failing action more than twice. No addresses or device identifiers in any memory or evidence file.
