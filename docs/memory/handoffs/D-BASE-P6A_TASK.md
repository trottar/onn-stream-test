---
memory_schema: 1
as_of: 2026-09-22
status: TASK HANDOFF — D-BASE-P6a, adopt `max_frame_size 90000` as the native stream profile default; one coherent product change plus one validation session; authorized by the user 2026-09-22 after their own perceptual check of the A1 arm
---

# D-BASE-P6a task handoff — make the 90 KB frame cap the default

**Where this stands (P6, verified 2026-09-22):** capping the encoder's largest frame at 90,000 bytes removed 100 % of the ≥ 80-packet frames and cut loss 7-9x on two independent pairs (A0 2,696 → A1 311; A0r2 2,896 → A1r2 408), with achieved bitrate, fps, encoder CPU and GPU power unchanged, and the small-frame loss buckets unchanged. The 60 KB cap bought nothing further on loss (299 vs 311), so the looser cap is the recommendation. The one cost the instruments cannot see — picture quality on the ~1 % of scene-change frames the cap binds on — was checked by the user on 2026-09-22 running the A1 arm (`encoder_overrides.any_override: true`, `max_frame_size_bytes: 90000` confirmed from `native-stream-status`): played for about a minute, no stutters, "could barely tell it was over the LAN." **That is a user-stated perceptual result, not instrument evidence; record it as such, in the user's words, and do not upgrade it.** With it, the user has decided to adopt the setting.

Read first: docs/memory/evidence/D_BASE_P6_FRAME_TAIL_CONTROL_2026-09-21.md (§4, §6, §9), docs/memory/patches/D-BASE-P6_FRAME_BYTE_COUNTERS_AND_ENCODER_KNOBS.md (how `PRIVYHUB_ENC_MAX_FRAME_SIZE` is wired today), companion/native_stream.py (the `h264_vaapi` argv builder and the `encoder_overrides` status block), the C1 explicit profile (wherever the static native stream parameters — 1280x720, 60 fps, 7000 kbps, GOP 15, `-bf 0`, `pkt_size=1200` — are declared), docs/memory/TOOLS.md (encoder knobs section), docs/PROJECT_STATUS.md and docs/README.md (the reference profile list).

**The change — one coherent product change, nothing else.** The cap moves from an environment override to a **profile field**: `max_frame_size_bytes: 90000` declared beside the other static parameters in the C1 explicit profile, so the `h264_vaapi` argv carries `-max_frame_size 90000` with **no environment variable set**. Keep the override for measurement: `PRIVYHUB_ENC_MAX_FRAME_SIZE` still overrides the profile when set, and **`PRIVYHUB_ENC_MAX_FRAME_SIZE=0` must run uncapped** (the argv omits `-max_frame_size`) so the P6 baseline can be re-run for comparison at any time; say in the status block which source is in force. `encoder_overrides` gains `default_max_frame_size_bytes: 90000` and `any_override` stays **false** when nothing but the profile is in force — that is the "default is in force" reading from now on. Apply this only to the `h264_vaapi` builder; if the profile object is shared with another encoder backend (the deferred Windows NVENC path), that backend **ignores the field with one log line** rather than translating it — no change to any other backend. `-bufsize` stays at its 7000k default; `PRIVYHUB_ENC_BUFSIZE_K` stays as it is; no pacing, no FEC, no client change, no other encoder flag touched. Inspect the exact current source and record the pre-patch hash before editing, as the patch rules require.

**Validation — one session, pre-registered.** Compile the Python, `git diff --check`, restart the companion (D-068) with 8765 checked free and **no `PRIVYHUB_ENC_*` variable in the environment** (`env | grep PRIVYHUB_ENC` must print nothing; record that it did). One 20-minute attract-mode session of the PS1 reference title, zero input, per TOOLS.md, BACK to end, with the P5/P6 instruments running (`native_frame_sizes.jsonl`, heartbeat v2, socket samples, T1 sampler). Before the hold, capture `native-stream-status`: the argv must contain `-max_frame_size 90000`, `encoder_overrides.any_override` must be `false`, and `default_max_frame_size_bytes` must be 90000; also confirm the launch line in `logs/games/native_video_alpha.log` carries the flag. **RUNTIME VALIDATED only if all of:** frames ≥ 80 packets **= 0** and max frame bytes ≤ 90,000; video `lost_packets` **≤ 600** (A1/A1r2 read 311/408; 600 is the bound with the observed ±15 % environment drift, and anything above it is a finding, not a pass); ≥ 80-packet frames absent from the bucketed loss table; achieved bitrate 6,900-7,050 kbps; rendered fps ≥ 59.8; 0 sequence resyncs; 0 onn socket drops; 0 relay send errors; encoder CPU median within 26-28 %. Then **one 60 s check with `PRIVYHUB_ENC_MAX_FRAME_SIZE=0`** to prove the uncapped path still runs and the argv omits the flag (a frame > 90,000 bytes appearing in the 60 s is the confirmation; the P6 smoke saw a 142 KB frame in 60 s). Restore the no-variable environment after it. Compare the validation session's per-minute loss series against A1 and A1r2 (Pearson and totals) and report it.

**Record and memory.** Raw numbers first in docs/memory/evidence/D_BASE_P6A_CAP_ADOPTED_<date>.md with the report, frame-size series, heartbeat slice, both status reads, the two argv lines and SHA-256s under evidence/d_base_p6a_<date>/. The user's perceptual check goes in that record in one sentence, attributed to the user and dated, separate from the measured section. Patch record in docs/memory/patches/ and PATCH_INDEX.md with the pre/post hashes. A **decision record** in docs/memory/decisions/ (the cap is the product default; 60 KB and VBV are measured alternatives, not chosen; the uncapped run stays available through the `=0` override). Update: **the reference profile wherever it is listed** — docs/PROJECT_STATUS.md, docs/README.md, docs/memory/MEMORY.md, the C1 profile documentation — adding `max_frame_size 90,000 bytes` to the 1280x720 / 60 fps / 7000 kbps / GOP 15 / no B-frames / 8+1 FEC / PT 96 / 1200-byte line, marked as adopted 2026-09-22 on P6 evidence; docs/KNOWN_ISSUES.md (the UDP burst pathology entry: resolved on the Linux host by the cap, with the mechanism named — frame-size tail → large-frame burst → wireless queue overflow; the Windows-era pathology stays a separate deferred note, not claimed resolved); docs/ROADMAP.md if it names the reference profile; docs/memory/evidence/RUNTIME_VALIDATION.md ledger; investigations/BASELINE_STREAM_HEALTH.md Step 3 (closed by adoption, with what remains: `prolonged_starvation_events`, R3b, thermal thresholds); CURRENT.md (fixed headings, `python3 tools/check_memory_health.py` healthy — trim; Next Action becomes R3b by hand / H1 / H2 / starvation events); handoffs/CURRENT_HANDOFF.md; investigations/ACTIVE.md; the dated memory file; TOOLS.md (the knob section: profile default 90000, `=0` for uncapped, how to read which source is in force). Teardown per TOOLS.md, companion left running on the new default. Never retry a failing action more than twice; if validation fails, **leave the profile change installed but classify DEVELOPMENT-ONLY with the failing number**, do not revert on your own, and say so in CURRENT.md. No addresses or device identifiers in any memory or evidence file.
