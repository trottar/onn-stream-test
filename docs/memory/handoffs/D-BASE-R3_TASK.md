---
memory_schema: 1
as_of: 2026-09-20
status: TASK HANDOFF — authorized D-BASE-R3 implementation prompt for the host-side Claude Code session; superseded once the evidence record exists
---

# D-BASE-R3 task handoff

Implement D-BASE-R3 (link-drop self-recovery) in /home/privyhub/Projects/onn-stream-test. It is AUTHORIZED; all constants are decided. Read first, in this order: docs/memory/CURRENT.md (Next Action), docs/memory/investigations/LINK_DROP_RECOVERY_DESIGN.md (the whole note — it is the spec; the Decisions section at the end holds every constant), docs/memory/TOOLS.md (host-shell driving rules, teardown order), docs/memory/handoffs/CURRENT_HANDOFF.md, docs/memory/evidence/D_BASE_R2_STALL_VISIBILITY_2026-09-20.md (the heartbeat you now consume), companion/games/emulator_manager.py, companion/native_session_io.py, companion/plugins/games.py, companion/diagnostics/c3_linux_actuator_probe.py (the encoder-only restart primitive to reuse, not copy), PrivyHub/app/src/main/java/streaming/NativeStreamActivity.kt and MainActivity.kt.

Build exactly what the design note says and nothing else. Constants, as named in the note: DESYNC_MS=1000, RECOVERY_CLEAN_TICKS=3 (startup gate stays 6), GIVE_UP_MS=120000, END_MS=1800000, RESTART_AFTER_MS=2000, RESTART_BACKOFF_MS=5000 doubling to 30000. Host state machine PLAYING / PAUSED_RECOVERING / PAUSED_SAVED / ENDED in the companion; desync trigger = controller-packet silence >= DESYNC_MS measured in the existing 250 ms controller loop, OR a client POST /plugins/games/native-stream-desync; pause and resume ONLY through EmulatorManager.pause()/resume() (never raw PAUSE_TOGGLE); native-stream-start must be accepted while already PAUSED_RECOVERING; resume only on native-stream-ready after the client passes the existing stabilization gate with RECOVERY_CLEAN_TICKS; encoder-only restart via the C3.L1 primitive with the backoff above, logged with the C3.L1 cycle record; recovery save at GIVE_UP_MS = the existing SAVE_STATE_SLOT 0 + copy mechanism with the copy going to <stem>.state.recovery — never a slot, _normalize_save_state_slot keeps rejecting anything but 1-3; graceful session end at END_MS; GET /plugins/games/status gains recovery:{state,since_ms,restarts,last_client_seen_ms,save_available,saved_at}; every state transition appended as a JSON line to logs/games/native_stream_recovery.log (host time, no addresses). Client: on last_output_age_ms >= DESYNC_MS or no RTP for DESYNC_MS, keep the last frame, show the stabilization overlay as "Reconnecting…", KEEP sending controller packets, post native-stream-desync once, then re-enter the stabilization gate with RECOVERY_CLEAN_TICKS and post native-stream-ready when it clears. Launcher: when status.recovery.save_available is true show one dialog — "The stream was lost at HH:MM and the game was saved automatically." with Resume from recovery save / Copy to slot 1 / Copy to slot 2 / Copy to slot 3 (existing occupied-slot confirmation applies, same slot-write path as a manual save) / Discard — also before launch of that game if the session ended at END_MS. Do NOT touch RtpH264Receiver resync/IDR logic, AvcLowLatencyDecoder, the 60 ms stale-drop policy, any streaming constant, FEC, audio, or the controller transport. Do not pause on audio starvation.

Patch discipline per docs/memory/patches/PATCH_PROTOCOL.md: one coherent change; compile the Python; git diff --check; build and install the APK with the Linux build/install path TOOLS.md records; restart the companion after the companion change (D-068) and check for an existing 8765 listener before starting it; record the APK and changed-file hashes.

Then run the validation plan from the note, autonomously, no user action, from a stable attract-mode session on the reference PS1 title, driven per TOOLS.md (MainActivity + tap, exit with KEYCODE_BACK, client sees game end before companion stops). Fault injection is host-side nftables exactly as the note gives it (udp dport {48100,48101} drop), for N = 0.5 s, 3 s, 15 s, 150 s, one run each, fresh session each, the table's expected column is the pass criterion. For every run keep raw evidence: logs/games/native_stream_recovery.log lines, the decoder session report and its gap row, heartbeat lines through the fault, and a uiautomator dump of the launcher at give-up for the 150 s run; after the 150 s run delete the rule, use RESUME PLAYING through the launcher, and confirm the recovery prompt appears and Resume from recovery save works. Always delete the nft table before teardown, even on failure. Teardown per TOOLS.md; leave no banner on the onn.

Write docs/memory/evidence/D_BASE_R3_LINK_DROP_RECOVERY_2026-09-20.md raw numbers first (per-run table: N, time to pause, time to resume after rule deletion, restarts, gate ticks, save file present, then classification), the patch record in docs/memory/patches/ and PATCH_INDEX.md, and update CURRENT.md (fixed headings; run python3 tools/check_memory_health.py and keep it healthy), MEMORY.md, handoffs/CURRENT_HANDOFF.md, investigations/ACTIVE.md, 2026-09-20.md, KNOWN_ISSUES.md (link-drop resilience block), and the design note's status line. Classify honestly: DEVELOPMENT if any run misses the expected column, RUNTIME VALIDATED only if all four pass. If a run cannot be completed, say INDETERMINATE with the reason and the exact command that failed; do not retry the same failing action more than twice. Never write IP addresses, ADB endpoints or device identifiers into any memory or evidence file.
