---
memory_schema: 1
as_of: 2026-09-22
status: TASK HANDOFF — D-BASE-P7, what `prolonged_starvation_events` counts; source read, one diagnostic client build (heartbeat fields only), one session; authorized by the user 2026-09-22 for an unattended overnight run
---

# D-BASE-P7 task handoff — name the starvation counter

**The open item.** `audio.prolonged_starvation_events` runs at 72-79/min on the old PC path and 35.8-40.7/min on the Opal path (`O1`), unchanged across a 90x range of session durations (`S1`, `S2`), fires with the audio queue at depth 0-8 — "full" by the queue's own scale — and is not moved by the `P2a` startup hold (`P2b`: five matched pairs, 125-157 either way). It is the last counter in the report whose meaning is unknown, and it halves when the path changes, so it is measuring something real about arrivals. Read first: docs/memory/evidence/D_BASE_P2B_STARVATION_SEPARATION_2026-09-21.md, D_BASE_P2_AUDIO_UNDERRUN_BURST_2026-09-20.md, MEMORY.md (the audio block and the `S1` soak block), docs/memory/evidence/linux_baseline_2026-09-15/privyhub_linux_baseline_33_audio_packet_cadence.txt and `_34_hybrid_audio_pacer.txt` (what the host's audio cadence was measured to be), PrivyHub/app/src/main/java/streaming/NativeAudioReceiver.kt, NativeStreamActivity.kt (heartbeat assembly), companion/games/native_stream_heartbeat.py, the host audio sender in companion/.

**Step 1 — read the code and write down, before any build, exactly what increments the counter:** the condition, its threshold in ms or packets, which thread evaluates it, how often that thread polls, and whether one starvation episode can increment it more than once (a poll-quantized counter fires once per poll while the condition holds, and then "events per minute" is a duty cycle, not an event count). State whether the queue-depth figure it is compared with is the same queue. Also write down what the report's `audio.lost_packets`, `concealment_*` and `avg_queue_residence_ms` are measured against, so the correlations below compare like with like. If the reading of the code already explains the counter (for instance, it fires on every poll where the queue holds fewer than N ms of audio while the track is playing, which at the sender's cadence must happen a fixed fraction of the time), say so in the record before the session and let the session confirm it.

**Step 2 — one diagnostic client build, heartbeat payload only.** Add to the heartbeat, cumulative, read from the same counters the report reads: `audio_prolonged_starvation_events`, `audio_lost_packets`, `audio_rx_packets`, `audio_concealment_events` (or whatever the report calls them), a spot `audio_queue_depth` and `audio_queue_ms`, and a per-tick **maximum audio inter-arrival gap in ms** (one `max()` in the receive loop reset per heartbeat — no new thread, no new sampling). Bump the heartbeat schema string; the companion passes the keys through. Nothing else changes; the counter's semantics are not touched. Build and install per TOOLS.md (gradlew under `PrivyHub/`), restart the companion (D-068) with 8765 checked free, `git diff --check`. Host side, read the sender's audio packet cadence from `relay.status()`/the companion status if it is exposed (packets per second and any send-timing figure); if it is not, do not add it — the client's inter-arrival gap is enough for this run.

**Step 3 — one 20-minute attract-mode session of the PS1 reference title, zero input, per TOOLS.md, BACK to end**, on the adopted profile (`any_override: false`), the P5/P6 instruments running.

**Analysis, per 2 s tick (n ≈ 600) and per minute (n = 20):** the starvation delta against the audio rx-packet count per tick (against the expected count at the sender's cadence — a deficit is arrival jitter, not loss, if `audio_lost_packets` did not move), the max inter-arrival gap, the audio loss delta, the video loss delta, queue depth/ms, and the starvation delta's own distribution (if it is a constant 1-2 per tick with tiny variance, it is quantized). Spearman for each; the ticks with the largest starvation deltas listed with their readings.

**Pre-registered reading, one of four.** **Definitional**: the delta per tick is near-constant and its rate equals the poll rate times a fraction that the code predicts — the counter is a duty cycle; the fix is a renamed or re-specified counter (count episodes, not polls) and the record proposes the exact semantics, does not implement them. **Arrival-gap driven**: starvation tracks the max inter-arrival gap or the rx deficit (|rho| ≥ 0.5) with audio loss flat — then the sender's cadence or the network's jitter is the cause, and the halving on the Opal path is the evidence to check it against; the levers are a slightly deeper audio prefill (latency cost, quantified in ms) or a sender-side pacing fix, the user's choice. **Loss-driven**: it tracks `audio_lost_packets` — then it is a second name for loss and the record says so. **None**: it tracks none of them; say what does distinguish the high ticks and stop. Include the `S2`/`O1` PC-vs-Opal halving in whichever reading is chosen: the reading must be able to explain it or say that it cannot.

Record raw numbers first in docs/memory/evidence/D_BASE_P7_STARVATION_COUNTER_<date>.md with the report, heartbeat file, frame-size series and SHA-256s under evidence/d_base_p7_<date>/; classify CHARACTERIZED / INDETERMINATE. Patch record in docs/memory/patches/ and PATCH_INDEX.md. Update CURRENT.md (fixed headings, `python3 tools/check_memory_health.py` healthy — trim), MEMORY.md (the counter's meaning as established — replace the "still unknown" line, do not append beside it), handoffs/CURRENT_HANDOFF.md, investigations/ACTIVE.md, KNOWN_ISSUES.md, TOOLS.md (the new heartbeat fields), the dated memory file. Teardown per TOOLS.md. Never retry a failing action more than twice. No addresses or device identifiers in any memory or evidence file.
