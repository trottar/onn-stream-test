---
memory_schema: 1
as_of: 2026-09-22
status: TASK HANDOFF — M1, bring the top-level repo narrative level with the evidence; documentation only, no source change, no git commit; authorized by the user 2026-09-22 for an unattended overnight run
---

# M1 task handoff — the repo narrative catches up with two days of evidence

**Why.** Since 2026-09-20 the evidence directory gained Group A, `R1`-`R5`, `P1`-`P7`, `C5`/`C5a`, `S1`-`S3`, `T1`, `B2`, `O1`, `H2-PREP`, and the loss column went from "deeply measured UDP burst pathology, deferred" to a named mechanism with an adopted fix. `docs/memory/` has kept up file by file; the top-level documents a new reader starts with have not, and the user's next checkpoint commit should not carry a README that still calls the pathology unexplained. **Documentation only.** No file outside `docs/` changes; **no `git commit`, no `git add`** — the checkpoint is the user's decision; this task leaves the tree ready and describes it.

Read first, in this order: docs/memory/CURRENT.md, MEMORY.md, handoffs/CURRENT_HANDOFF.md, evidence/RUNTIME_VALIDATION.md, then docs/PROJECT_STATUS.md, docs/ROADMAP.md, docs/KNOWN_ISSUES.md, docs/README.md, docs/memory/investigations/{ACTIVE,DEFERRED,BASELINE_STREAM_HEALTH}.md, decisions/, roadmap/. **The evidence records are authoritative; when a summary and a record disagree, the record wins and the summary is corrected — never the reverse.**

**Do, in order:**

1. **docs/PROJECT_STATUS.md.** Current validated state as of the newest records: Linux host wired to the Opal is the production topology (`B2`); the reference profile line gains `max_frame_size 90,000 bytes` (`P6a`, and `S3`'s classification); the loss mechanism in two sentences with the record names; the recovery state machine (`R3`/`R3a`, constants as set), heartbeat v2 (`R5`), report fields (`R4`), thermal telemetry (`T1`), frame-size counters (`P5`/`P6`); what is runtime validated, development-only, deferred, active, next — each with the record that says so. Remove or mark superseded any statement the records have overtaken (the Windows companion as "temporary server" is history; the "current technical item C1 explicit stream profiles / C1_DESIGN_MINIMAL_EXPLICIT_PROFILE_SCHEMA" line is long done — say what replaced it).
2. **docs/KNOWN_ISSUES.md.** The UDP burst/gap/duplication entry becomes two entries: the Windows-era pathology (deferred, never replayed on Linux, and now moot for the Linux host unless the acceptance suite is rerun — say that), and the Linux-path loss (resolved by the cap; residual per `S3`; reopen conditions from CURRENT.md's falsified list). Add the open items with their current state: `R3b` by hand (permission, not root), `END_MS`, `prolonged_starvation_events` (per `P7`'s reading), thermal thresholds deferred, `host_link`, the onn's absent thermal zones and radio counters, Dropbear's RSA-only host key on the Opal (documented in TOOLS.md, note only).
3. **docs/ROADMAP.md.** Confirm it is still the authoritative Linux-first roadmap v3; update the position marker to what CURRENT.md says (baseline stream health closed or at its residual, headless cutover next, then whatever the roadmap lists after it); do not restructure the roadmap.
4. **docs/README.md and the repository README** (if the top-level README repeats the project description): the documentation map gains the evidence directory's newest records by name and the `docs/memory/handoffs/` convention (task handoffs written by the user's assistant, run by Claude Code, recorded in evidence — one line); the ports table is checked against the source (`companion/`) and corrected if any port moved; the reference-profile paragraph updated as in item 1.
5. **docs/memory/MEMORY.md curation.** It is ~36 KB. Curate, do not append: merge duplicated statements of the same fact, mark superseded blocks as superseded with the newer record named, keep every durable rule, drop nothing that a record still relies on. Target under 30 KB without losing a fact; list in the dated memory file what was merged.
6. **investigations/ACTIVE.md and DEFERRED.md** reconciled with CURRENT.md's Next Action; `evidence/RUNTIME_VALIDATION.md` gains any record from 2026-09-20/22 it is missing, one line each, with classification.
7. **Checkpoint description**, not a commit: `git status --short | wc -l`, the list of changed top-level files, and a proposed commit message in docs/memory/CHECKPOINT_PROPOSAL_<date>.md (title plus a paragraph naming the record files, so the user can `git add -A && git commit -F` it if they choose). Confirm with `git status` that nothing under the ROM/ISO/BIOS/keys paths and nothing under `logs/` is staged or untracked-and-unignored; if something is, name it and do not touch it.
8. `python3 tools/check_memory_health.py` healthy; CURRENT.md, handoffs/CURRENT_HANDOFF.md and the dated memory file updated to say M1 ran and what it changed.

Never retry a failing action more than twice. No addresses, MACs, SSIDs or device identifiers anywhere; if any top-level document already contains one, redact it and say so in the dated memory file.
