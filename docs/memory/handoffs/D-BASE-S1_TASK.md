---
memory_schema: 1
as_of: 2026-09-21
status: TASK HANDOFF — D-BASE-S1, overnight soak on the current (PC-path) topology; measurement only, no code change; runs after D-BASE-P2b; authorized 2026-09-21 for an unattended overnight run
---

# D-BASE-S1 task handoff — overnight soak

Run only after `D-BASE-P2b` is recorded in docs/memory/CURRENT.md. Unattended, ask nothing. This is the last run on the host -> Windows PC -> Opal -> onn path before the host moves to the Opal in the morning; it is the reference the morning's `B2` will be compared against, and it answers questions no 2-minute session can. Read first: docs/memory/CURRENT.md, docs/memory/TOOLS.md, docs/memory/evidence/GROUP_A_DIAGNOSTIC_AUDIT_2026-09-20.md (A2.3: spike rate flat with elapsed time, measured only to ~5 min), D_BASE_P2_AUDIO_UNDERRUN_BURST_2026-09-20.md (`prolonged_starvation_events` scales with duration, 749 in the 300 s+ bucket, unexplained), C5A_IDR_REJECTION_COUNT_2026-09-21.md (the counters need natural resyncs), D_BASE_R4_REPORT_VISIBILITY_2026-09-20.md (rotation limits), companion/games/link_drop_recovery.py.

**Sessions.** Four attract-mode sessions of the PS1 reference title, **30 minutes each** (two hours of streaming), zero input, per TOOLS.md, BACK to end, full teardown between, at least 2 minutes idle between sessions. Recovery is live: if a natural outage pauses the game, let it recover; if it reaches give-up (2 min) or `END_MS`, that is a result — record it, discard the recovery save on teardown, continue with the next session. Every session must be ended cleanly; nothing left in state T, no banner on the onn at the end of the night, companion stopped last.

**Host-side sampling every 30 s during each session**, to a JSON-lines file per session under evidence/d_base_s1_<date>/: CPU temperature and package power if `/sys/class/thermal` or `/sys/class/hwmon` expose them, load average, the encoder process's CPU % and RSS, the companion's RSS and thread count, the FEC relay's `status()` counters, `GET /plugins/games/status` (`recovery` block, `native-stream-status` newest heartbeat), and the sizes of `logs/games/native_stream_heartbeat.log` and `native_stream_recovery.log` and their archive directory. Reject nothing; a session that dies is a finding.

**From each session's report:** everything `D-BASE-R4` and `C5a` added, `stream_discontinuities` with every C5a per-row field, `slow_events_top_gap` / `top_latency`, `audio.tick_series` (note: capped at 200 s — say so), `prolonged_starvation_events`, `underruns`, `avg_queue_residence_ms`, and the rate columns per minute. Also the heartbeat log: `last_output_age_ms` and `rendered_frames` at 2 s resolution for the whole 30 minutes, which gives rendered fps and stall count **per minute across the session** — the time series A2.3 could not build.

**Questions to answer, each with the numbers:** (1) does anything degrade with elapsed time over 30 minutes — spike rate, rendered fps, output gaps, loss, encoder CPU, temperature, RSS — per-minute series and a Spearman against elapsed minute for each; (2) `prolonged_starvation_events` growth: linear with time, or stepwise, and does it coincide with anything in the video series or the heartbeat; (3) how many natural discontinuities occurred in two hours, their `resync_to_idr_ms` and `rejected_idr_aus` — apply C5a's pre-registered rule if six or more with any over 250 ms, else report them and leave C5a INDETERMINATE; (4) recovery events: any `desync_pause`, their trigger, duration and outcome; (5) rotation: did either log rotate, and did the archive family stay within its limit; (6) the loss shape over two hours — packets per gap event, max forward gap, reordering — as the PC-path reference for the morning. State plainly which results are time-dependent and which are not.

Record raw numbers first in docs/memory/evidence/D_BASE_S1_OVERNIGHT_SOAK_<date>.md (classify CHARACTERIZED / INDETERMINATE, name every session that did not complete), artifacts with SHA-256s under evidence/d_base_s1_<date>/ (keep the reports, heartbeat and recovery logs, the sampling files; do not copy anything containing addresses — the FEC status carries none, `status` must be filtered if it does). Update CURRENT.md (fixed headings, `python3 tools/check_memory_health.py` healthy — trim), MEMORY.md, handoffs/CURRENT_HANDOFF.md (this is what the morning session reads first: state that the host is still on the PC path and that `D-BASE-B2_TASK.md` is next once the cable moves), investigations/ACTIVE.md, the dated memory file, KNOWN_ISSUES.md. Teardown per TOOLS.md. Never retry a failing action more than twice. No IP addresses, MACs, SSIDs, ADB endpoints or device identifiers in any memory or evidence file.
