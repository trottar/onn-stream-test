---
memory_schema: 1
as_of: 2026-09-22
status: TASK HANDOFF — D-BASE-S3, three-hour soak on the adopted 90 KB cap; instruments only, no code change; authorized by the user 2026-09-22 for an unattended overnight run
---

# D-BASE-S3 task handoff — three hours on the new default

**Why.** `P6a` validates the cap over 20 minutes. `S2` (2026-09-21) is the only multi-hour session on record and it ran uncapped on the old PC-wired path. The adopted profile has never run for hours on the production path, and three questions only a long session answers: does the cap hold its 7-9x for hours or does the environment drift eat it; what the **residual** loss (A1/A1r2: 311/408 per 20 min, ~15-20/min) tracks now that the tail is gone; and whether the new frame-size log rotates cleanly mid-session. Read first: docs/memory/evidence/D_BASE_P6A_CAP_ADOPTED_<date>.md (its classification governs the wording of this record, not whether it runs), D_BASE_P6_FRAME_TAIL_CONTROL_2026-09-21.md §5-§7, D_BASE_S2_THREE_HOUR_SESSION_2026-09-21.md (the comparison arm), D_BASE_R5_HEARTBEAT_LOSS_COUNTERS_2026-09-21.md (slicing across rotation — do not slice the heartbeat log by byte offset), docs/memory/TOOLS.md.

**No code change.** Confirm before the hold, from `native-stream-status`: argv carries `-max_frame_size 90000`, `encoder_overrides.any_override: false`, `env | grep PRIVYHUB_ENC` empty. Record all three.

**One session: 180 minutes, attract mode, PS1 reference title, zero input, per TOOLS.md, BACK to end.** Instruments: heartbeat v2, `native_frame_sizes.jsonl`, the onn socket sampler (P5), the T1 host sampler, the Opal read-only sampler from `O1` at 20 s (channel utilization, the onn's station row, load) if `ssh -o BatchMode=yes opal uptime` succeeds in one try — if it does not, skip it and say so, do not retry.

**Per hour and per minute, record:** video `lost_packets` (180-number series and three hourly totals), forward-gap events and max gap, sequence resyncs, frames ≥ 40 / ≥ 80 packets and max frame bytes (must be 0 and ≤ 90,000 in every minute — one violation is a finding that outranks the rest), achieved bitrate, rendered fps, `spike_20_ms`/min, `max_output_gap_ms`, audio loss and `prolonged_starvation_events`/min, onn socket drops, host hottest sensor and GPU power, onn thermal status, RetroArch and companion RSS (S2 found file-backed warm-up, not a leak — say whether that still holds), Opal channel utilization if sampled. Note every rotation of `native_frame_sizes.jsonl` and the heartbeat log (time, size, that the archive copy is intact and the series continues without a gap across it).

**Residual-loss analysis, pre-registered.** On 10 s windows (n ≈ 1,080) and per minute (n = 180): Spearman of loss against max packets-per-frame, frames in the 40-59 and 60-79 buckets, audio loss, and Opal channel utilization if sampled; the bucketed loss table from `P6` §5 (windows assigned by their largest frame; `< 40`, 40-59, 60-79, ≥ 80). Readings: **the residual still tracks frame size** if loss per window rises monotonically across the three remaining buckets with |rho| ≥ 0.4 — then a tighter cap or pacing inside a larger budget is a live lever and the record says which bucket carries it; **the residual is a floor** if loss is flat across buckets and tracks nothing (|rho| < 0.3 on all series) — then the loss column is closed at this level and the record says so; **hour-to-hour drift** is flagged if any hour's loss exceeds twice another's with the frame distribution unchanged, and it is compared with the ±15 % `P6` saw. Compare the whole session with `S2` (loss/min, spikes/min, starvation/min, forward gaps) and with `A1`/`A1r2` (first 20 minutes: Pearson and total).

**Classification:** the profile is **SOAK VALIDATED** if every minute has zero ≥ 80-packet frames, session loss per minute stays ≤ 30 (1.5x the A1r2 rate), 0 resyncs beyond natural 128-packet jumps, no rotation loses data, thermals plateau as `T1` found; otherwise state which number failed and leave the profile as it is. Record raw numbers first in docs/memory/evidence/D_BASE_S3_CAP_SOAK_<date>.md with the report, per-minute series, frame-size and socket series, samplers and SHA-256s under evidence/d_base_s3_<date>/. Update CURRENT.md (fixed headings, `python3 tools/check_memory_health.py` healthy — trim), MEMORY.md (the residual reading, the drift, the rotation), evidence/RUNTIME_VALIDATION.md, handoffs/CURRENT_HANDOFF.md, investigations/ACTIVE.md and BASELINE_STREAM_HEALTH.md Step 3, the dated memory file. Teardown per TOOLS.md, companion left running on the default. Never retry a failing action more than twice; INDETERMINATE with the exact command otherwise. No addresses, MACs, SSIDs or device identifiers in any memory or evidence file.
