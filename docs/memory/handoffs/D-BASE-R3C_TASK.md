---
memory_schema: 1
as_of: 2026-09-22
status: TASK HANDOFF — D-BASE-R3c, the launcher/recovery semantics the user decided 2026-09-22 (never load a recovery state into a live core; Resume tile when a session is live; recovery prompt only when none is), preceded by the one probe that decides whether a fresh-core load plays; authorized by the user 2026-09-22 for an unattended overnight run
---

# D-BASE-R3c task handoff — recovery never touches a live core

**The decision (user, 2026-09-22), verbatim in substance:** a recovery state is **never loaded into a running core**. When a live game session exists (paused because the player left the stream screen, or otherwise), the launcher tile for that title reads **Resume** and tapping it resumes that session with no state load. The recovery prompt (resume from recovery save / copy to a slot / discard) appears **only when no live session exists** and a `.state.recovery` file does; "resume from recovery save" then **launches a fresh RetroArch and loads the state into it**. `native-stream-stop` keeps its current meaning (end the stream, pause the game, keep the session) — that is a feature, not the bug. The bug was `recovery-resume` acting on a live core; see evidence/R3B_POST_RUN_SESSION_REUSE_2026-09-22.md.

Read first: that finding, D_BASE_R3B_NFTABLES_LINK_DROP_2026-09-22.md (open items), investigations/LINK_DROP_RECOVERY_DESIGN.md, companion/plugins/games.py (`native-stream-stop`, `recovery-resume`, the launch tile data the client reads), companion/games/emulator_manager.py (`load_recovery_state`, launch, pause/resume, the state-file paths), PrivyHub/app/src/main/java/.../MainActivity.kt (the tile and the prompt), docs/memory/TOOLS.md (adb driving, RESUME PLAYING, the framemd5 method from Group A / A3-live), evidence/GROUP_A_DIAGNOSTIC_AUDIT_2026-09-20.md §A3.

**Part 1 — the probe, first, host only, no TV needed.** Copy `data/games/retroarch/states/Beetle PSX HW/Tekken 3 (USA).state.recovery` (and its `.png`) to evidence/d_base_r3c_<date>/ with SHA-256 before anything touches it; it is the only copy of the N150 give-up state. Then, with the companion idle (`active false`, no RetroArch): launch the PS1 reference title through the companion's normal launch path, wait for the window, pause, load the copied recovery state through the same load path the fix will use (whatever `load_recovery_state` does today, against this **fresh** core), unpause, and run 30 s of `x11grab -> framemd5` on the managed window as A3-live did, counting distinct frame hashes and the IDR-size dispersion from `native_frame_sizes.jsonl` if the relay is running (it need not be). Do the same once more loading the state **while the core is running, not paused**, if the manager's API allows it; if it does not, say so. Reading: **fresh-core load plays** if distinct frames over 30 s are in the hundreds and the IDR spread is in the gameplay band (53-74 KB in R3b), and the fix proceeds; **fresh-core load also loops** (distinct frames in the single digits, spread under 5 KB) if the core/state interaction under GL hardware rendering is the cause — then **stop before Part 2**, record INDETERMINATE for the fix, and write down what the recovery save would have to change (a software-rendered state? a core option?) as the user's decision. End the probe session cleanly; discard nothing.

**Part 2 — the change, only if Part 1 played.** One coherent change across companion and client, nothing else:

- Companion `recovery-resume`: if a live session exists for **any** title, end it cleanly first (the same path a normal stop uses, with the save-files flush the manager already does), then launch the requested title fresh and load the recovery state into it; never call `load_recovery_state` against a pre-existing session. `copy-to-slot` copies the file into the chosen slot without launching; `discard` deletes `.state.recovery` and its `.png`. On a **successful** resume-from-recovery (state loaded, core running, first RTP seen), delete the `.state.recovery` and `.png` so the prompt does not recur; on failure keep them and report the error to the client. Expose in the tile data what the client needs: `live_session: {title, paused}` and `recovery_available: bool` per title.
- Client tile: `live_session` for this title → the tile's primary action is **Resume** (opens the stream against the running session; no prompt, no load). No live session and `recovery_available` → the three-option prompt before launch. Neither → plain launch. A live session for a **different** title → the tile launches normally and the companion ends the other session first (say so in a one-line toast if the client has one; otherwise silent).
- No change to the recovery state machine, its constants, the encoder, the stream, or `native-stream-stop`.

Inspect exact current source and record pre-patch hashes; compile Python; `git diff --check`; Android build and install per TOOLS.md; restart the companion (D-068) with 8765 checked free.

**Validation, all driven over adb per TOOLS.md, the onn on and paired (fixed port 5555 per H1-VERIFY):**

1. **Live session → Resume.** Launch the reference title, open the stream, play 30 s of attract mode, BACK to the launcher (session stays, paused). The tile must read Resume; tapping it must reopen the stream on the **same RetroArch pid** with no `load_state` line in `save_state_probe.txt`, output resuming within 3 s.
2. **No live session + recovery file → prompt → fresh launch.** Stop the session (companion idle), restore the copied `.state.recovery` into place. Launch the title from the launcher: the prompt must appear (uiautomator dump filtered), choose resume-from-recovery: a **new** RetroArch pid, the state loaded (probe line), stream opens, and the picture moves — 60 s of `native_frame_sizes.jsonl` with IDR spread in the gameplay band and the onn's report clean; the `.state.recovery` must be gone afterwards, and a second launch must show no prompt.
3. **Discard.** Restore the file again, launch, choose discard: the file and `.png` are deleted, the game launches plain.
4. **Copy to slot.** Restore, launch, choose copy-to-slot: the slot file's SHA-256 equals the recovery file's; the game launches plain; the recovery file is deleted after the copy (state that choice in the record).
5. **Cross-title.** With a live paused session of the reference title, launch a different title: the old session ends cleanly (no orphan pid, save files flushed), the new one launches.

RUNTIME VALIDATED only if all five pass; otherwise DEVELOPMENT-ONLY with the failing check named, change left installed, and CURRENT.md saying so. Teardown per TOOLS.md, the recovery copy under evidence/ retained, no recovery file left in the states directory.

**Record and memory.** docs/memory/evidence/D_BASE_R3C_RECOVERY_SEMANTICS_<date>.md (probe first, then the five checks) with artifacts and SHA-256s under evidence/d_base_r3c_<date>/; patch record and PATCH_INDEX.md; a decision record in decisions/ stating the semantics above as the user's decision; investigations/LINK_DROP_RECOVERY_DESIGN.md updated (the prompt's precondition and the live-session rule); CURRENT.md (fixed headings, `python3 tools/check_memory_health.py` healthy — trim), MEMORY.md (the rule: recovery never loads into a live core), handoffs/CURRENT_HANDOFF.md, investigations/ACTIVE.md, KNOWN_ISSUES.md (the two R3b open items closed or narrowed), TOOLS.md (the tile states and what RESUME PLAYING now means), the dated memory file. Never retry a failing action more than twice; INDETERMINATE with the exact command otherwise. No addresses, ADB endpoints or device identifiers in any memory or evidence file.
